[package]
name = "zerowall-mcp-proxy"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "zerowall-mcp-proxy"
path = "../../src/mcp_proxy_main.rs"

[dependencies]
fs2 = "0.4"
reqwest = { version = "0.13", default-features = false, features = ["blocking", "native-tls"] }
serde_json = "1"
