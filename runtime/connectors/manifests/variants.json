{
  "domain": "variants",
  "toolCount": 18,
  "tools": [
    {
      "name": "cadd_position_scores",
      "description": "CADD scores for ALL possible single-nucleotide substitutions at one genomic position (up to 3 records, one per alt allele)."
    },
    {
      "name": "cadd_range_scores",
      "description": "CADD scores for every SNV in a genomic window [start, end] (inclusive, max 100 bp — split larger spans into consecutive windows)."
    },
    {
      "name": "cadd_variant_score",
      "description": "CADD deleteriousness score for one SNV (ref>alt at chrom:pos)."
    },
    {
      "name": "clinvar_get_records",
      "description": "Fetch full ClinVar records for a batch of VCV/RCV accessions or bare variation IDs."
    },
    {
      "name": "clinvar_search",
      "description": "Search ClinVar directly (live NCBI, not gnomAD's snapshot) and return matching variation records with clinical significance, review status and gold stars."
    },
    {
      "name": "clinvar_variant_by_rsid",
      "description": "All ClinVar variation records that reference a dbSNP rsID, with full classifications (an rsID can map to several VCVs — one per alternate allele, e.g. rs121913529 covers KRAS G12D/G12V/G12A)."
    },
    {
      "name": "clinvar_variants",
      "description": "List ClinVar variants in a gene as mirrored by gnomAD, with clinical significance, review status and gold stars."
    },
    {
      "name": "dbsnp_get_rsids",
      "description": "Canonical dbSNP RefSNP records for a batch of rsIDs: GRCh38+GRCh37 placements, alleles, gene context, per-study allele frequencies, and ClinVar cross-references."
    },
    {
      "name": "dbsnp_search_by_region",
      "description": "List dbSNP rsIDs in a genomic window (esearch db=snp positional index — NCBI Variation Services has no region endpoint)."
    },
    {
      "name": "gene_constraint",
      "description": "gnomAD gene constraint metrics: pLI, observed/expected LoF-missense- synonymous counts with oe ratios + 90% CI bounds, and per-class z-scores."
    },
    {
      "name": "gene_variants",
      "description": "List ALL gnomAD short variants in a gene (complete listing — can be thousands of rows for large genes)."
    },
    {
      "name": "get_structural_variant",
      "description": "Look up one gnomAD structural variant by its release-specific SV ID."
    },
    {
      "name": "get_variant",
      "description": "Look up one gnomAD short variant by ID and return its population frequencies."
    },
    {
      "name": "liftover_variant",
      "description": "Map a variant ID between reference builds (GRCh37 <-> GRCh38) using gnomAD's liftover table."
    },
    {
      "name": "mitochondrial_variants",
      "description": "List gnomAD mitochondrial variants with heteroplasmy-aware counts (``ac_het``, ``ac_hom``, ``max_heteroplasmy``) for a mitochondrial gene OR a chrM coordinate window."
    },
    {
      "name": "region_variants",
      "description": "List ALL gnomAD short variants in a genomic region (max 1 Mb — split larger regions into consecutive windows)."
    },
    {
      "name": "search_variants",
      "description": "Search gnomAD for variant IDs matching a query string (rsID like ``rs7412``, a variant ID, or a prefix)."
    },
    {
      "name": "structural_variants",
      "description": "List gnomAD structural variants (deletions, duplications, insertions, inversions, CNVs...) overlapping a gene."
    }
  ]
}
