{
  "domain": "literature",
  "package": "mcp_literature",
  "derivedFrom": "bio-tools/lib/mcp_literature/server.py",
  "toolCount": 9,
  "tools": [
    {
      "name": "arxiv_get_papers",
      "description": "Batch-fetch arXiv paper metadata (incl. abstracts) by ID — one paced request for up to 100 papers.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "arxiv_ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "up to 100 arXiv IDs in any common form — ``2103.14030``, versioned ``2103.14030v2``, old-style ``q-bio/0601001``, ``arXiv:`` prefixed, or abs/pdf URLs. Unversioned IDs resolve to the latest version."
          }
        },
        "required": [
          "arxiv_ids"
        ]
      },
      "returns": "Returns ``{n_requested, n_found, duplicates, not_found, records}`` — ``duplicates`` lists inputs that resolved to an already-returned paper (e.g. bare + versioned spellings of one id); records in the requested order, same shape as ``arxiv_search`` records. Unknown AND malformed IDs are listed in ``not_found`` (the API silently skips unknowns and rejects whole batches over malformed ones; this tool does neither). Withdrawn papers still return metadata — check ``comment`` for withdrawal notes.",
      "upstreams": [
        "arxiv_fetch"
      ]
    },
    {
      "name": "arxiv_search",
      "description": "Search arXiv preprints (physics, math, CS, stats, q-bio, ...) via the official Atom API.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": [
              "string",
              "null"
            ],
            "description": "arXiv query string. Plain terms search all fields; field prefixes ``ti:`` (title), ``au:`` (author), ``abs:`` (abstract) and booleans ``AND``/``OR``/``ANDNOT`` work, e.g. ``ti:\"protein language model\" AND abs:structure``. Optional if category or a date range is set."
          },
          "category": {
            "type": [
              "string",
              "null"
            ],
            "description": "arXiv category code to AND in, e.g. ``q-bio.GN``, ``q-bio.PE``, ``cs.LG``, ``stat.ML``."
          },
          "date_from": {
            "type": [
              "string",
              "null"
            ],
            "description": "earliest submission date, ``YYYY-MM-DD`` (inclusive)."
          },
          "date_to": {
            "type": [
              "string",
              "null"
            ],
            "description": "latest submission date, ``YYYY-MM-DD`` (inclusive)."
          },
          "start": {
            "type": "integer",
            "description": "result offset for paging (0-based). The API paces 3 s between requests — page politely, don't hammer.",
            "default": 0
          },
          "max_results": {
            "type": "integer",
            "description": "page size (default 25, max 100 per call).",
            "default": 25
          },
          "sort_by": {
            "type": "string",
            "description": "``relevance`` (default), ``submittedDate``, or ``lastUpdatedDate``.",
            "default": "relevance"
          },
          "sort_order": {
            "type": "string",
            "description": "``descending`` (default) or ``ascending``.",
            "default": "descending"
          }
        }
      },
      "returns": "Returns ``{search_query (the exact query sent), api_total (arXiv's own total match count), start_index, n_records_returned, records_truncated, sort_by, sort_order, records}``. Each record: ``{arxiv_id, version, id_versioned, title, abstract, authors, published, updated, primary_category, categories, doi, journal_ref, comment, abs_url, pdf_url}``. ``doi``/``journal_ref`` are present only after journal publication. Malformed queries raise an error (arXiv's HTTP-200 error feed is detected, never returned as data).",
      "upstreams": [
        "arxiv_fetch"
      ]
    },
    {
      "name": "openalex_citations",
      "description": "List works that CITE a given work (incoming citations), via OpenAlex's citation graph.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "work_id": {
            "type": "string",
            "description": "OpenAlex W-id, openalex.org URL, or DOI of the cited work (DOIs cost one extra resolution request)."
          },
          "sort": {
            "type": "string",
            "description": "``cited_by_count`` (default — most influential citers first), ``publication_date`` (newest first), or ``relevance`` (OpenAlex native order here).",
            "default": "cited_by_count"
          },
          "max_records": {
            "type": "integer",
            "description": "cap on returned citing works (default 50, ceiling 500). Heavily-cited papers have tens of thousands of citers — check ``api_total`` and ``records_truncated``.",
            "default": 50
          },
          "include_abstracts": {
            "type": "boolean",
            "description": "include reconstructed abstracts per record (bulky; default false; license-gated — see ``openalex_search_works``: only verified-open licenses reconstruct, others carry ``abstract_policy``).",
            "default": false
          }
        },
        "required": [
          "work_id"
        ]
      },
      "returns": "Returns ``{work_id, api_total, n_records_returned, records_truncated, records}`` — ``api_total`` is the true citing-work count (OpenAlex's ``cited_by_count`` for the work); records are lean work records. DOI inputs resolve to the most-cited claimant when upstream has duplicate DOI tags (``doi_claimants`` + ``doi_resolution_note`` appear in the output when that happens — see ``openalex_get_work``).",
      "upstreams": [
        "openalex_works"
      ],
      "errorCases": [
        {
          "case": "openalex_key_required",
          "expectedError": {
            "error": "openalex_key_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "openalex_get_author",
      "description": "Fetch one OpenAlex author profile plus their top-cited works.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "author_id": {
            "type": "string",
            "description": "OpenAlex A-id (``A5065535610``), openalex.org URL, or ORCID (``0000-0002-9943-7557`` or orcid.org URL). CAVEAT: OpenAlex's ORCID pointer sometimes resolves to a sparse duplicate profile — prefer the A-id of the right profile from ``openalex_search_authors`` (check works_count)."
          },
          "works_sample": {
            "type": "integer",
            "description": "number of the author's most-cited works to attach (default 10, max 200; 0 skips the extra request).",
            "default": 10
          }
        },
        "required": [
          "author_id"
        ]
      },
      "returns": "Returns the author record (see ``openalex_search_authors``) plus ``counts_by_year`` (works + citations per year), ``top_works_total`` (the author's true total works count per the works index) and ``top_works`` (lean work records sorted by citations, no abstracts).",
      "upstreams": [
        "openalex_works"
      ],
      "errorCases": [
        {
          "case": "openalex_key_required",
          "expectedError": {
            "error": "openalex_key_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "openalex_get_work",
      "description": "Fetch one OpenAlex work in full — metadata, abstract (reconstructed from the inverted index), OA locations, and the complete outgoing reference ID list.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "work_id": {
            "type": "string",
            "description": "OpenAlex W-id (``W2981137429``), openalex.org URL, bare DOI (``10.1038/s41586-019-1711-4``) or doi.org URL."
          }
        },
        "required": [
          "work_id"
        ]
      },
      "returns": "Returns the lean work record (see ``openalex_search_works``) plus ``abstract`` (reconstructed ONLY when the work's declared license verifies open — cc-by/cc-by-sa/cc0/public-domain; otherwise null with an ``abstract_policy`` link-out note and the declared license in ``abstract_license``; also null when the publisher withholds the inverted index entirely), ``referenced_works`` (outgoing reference W-ids, use ``openalex_references`` to hydrate them) and ``counts_by_year`` (citations per year, last decade). DOI lookups resolve via OpenAlex's claimant filter — upstream sometimes tags several works with one DOI; the most-cited claimant is selected and, when more than one exists, ``doi_claimants`` (all of them, with citation counts) + ``doi_resolution_note`` are included so the choice is visible. Raises a not-found error for unknown IDs/DOIs.",
      "upstreams": [
        "openalex_works"
      ],
      "errorCases": [
        {
          "case": "openalex_key_required",
          "expectedError": {
            "error": "openalex_key_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "openalex_references",
      "description": "List the works a given work CITES (outgoing references), hydrated to full metadata in reference-list order.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "work_id": {
            "type": "string",
            "description": "OpenAlex W-id, openalex.org URL, or DOI."
          },
          "max_records": {
            "type": "integer",
            "description": "cap on references hydrated to full records (default 100, ceiling 500; hydration is batched 50 per request). The complete un-hydrated ID list is always returned.",
            "default": 100
          }
        },
        "required": [
          "work_id"
        ]
      },
      "returns": "Returns ``{work_id, n_references (true outgoing-reference count), n_records_returned, records_truncated, references_not_hydrated, reference_ids (ALL outgoing W-ids), records}``. Records are lean work records (no abstracts) in the order the source work lists them. References OpenAlex has no standalone record for (merged/deleted works) cannot be hydrated — their IDs are listed explicitly in ``references_not_hydrated``, never silently dropped.",
      "upstreams": [
        "openalex_works"
      ],
      "errorCases": [
        {
          "case": "openalex_key_required",
          "expectedError": {
            "error": "openalex_key_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "openalex_search_authors",
      "description": "Search OpenAlex author profiles by name.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "author name, e.g. ``\"David Liu\"`` — matches display name and alternatives. Expect homonyms: check affiliations, topics and ORCID before trusting a match."
          },
          "max_records": {
            "type": "integer",
            "description": "cap on returned profiles (default 25, ceiling 500).",
            "default": 25
          }
        },
        "required": [
          "query"
        ]
      },
      "returns": "Returns ``{query, api_total, n_records_returned, records_truncated, records}``; each record: ``{author_id, name, orcid, works_count, cited_by_count, h_index, i10_index, affiliations[{institution, years}], last_known_institutions, top_topics}``. Use the ``author_id`` with ``openalex_get_author`` for a works sample.",
      "upstreams": [
        "openalex_works"
      ],
      "errorCases": [
        {
          "case": "openalex_key_required",
          "expectedError": {
            "error": "openalex_key_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "openalex_search_works",
      "description": "Search OpenAlex scholarly works (all disciplines, ~250M records) with year/type/OA/venue filters.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": [
              "string",
              "null"
            ],
            "description": "free-text search over title + abstract + full text, e.g. ``\"prime editing off-target\"``. Optional if at least one filter is set."
          },
          "year_from": {
            "type": [
              "integer",
              "null"
            ],
            "description": "earliest publication year (inclusive), e.g. ``2020``."
          },
          "year_to": {
            "type": [
              "integer",
              "null"
            ],
            "description": "latest publication year (inclusive)."
          },
          "work_type": {
            "type": [
              "string",
              "null"
            ],
            "description": "OpenAlex type filter — common values ``article``, ``review``, ``preprint``, ``book-chapter``, ``dataset``, ``dissertation``."
          },
          "open_access_only": {
            "type": "boolean",
            "description": "if true, only works with a free-to-read copy.",
            "default": false
          },
          "venue": {
            "type": [
              "string",
              "null"
            ],
            "description": "restrict to one journal/repository — OpenAlex S-id (``S106963461``), openalex.org URL, ISSN (``1087-0156``), or a plain venue name (``\"Nature Biotechnology\"``; resolved to the top sources-index hit, surfaced in ``venue_resolved`` so the choice is visible — pass an exact ID to skip resolution)."
          },
          "sort": {
            "type": "string",
            "description": "``relevance`` (default; needs a query, otherwise OpenAlex native order), ``cited_by_count``, or ``publication_date``.",
            "default": "relevance"
          },
          "max_records": {
            "type": "integer",
            "description": "cap on returned records (default 50, hard ceiling 500; pages of 200 are walked politely).",
            "default": 50
          },
          "include_abstracts": {
            "type": "boolean",
            "description": "if true, each record carries ``abstract`` (reconstructed from OpenAlex's inverted index — but ONLY for works whose declared license verifies open (cc-by/cc-by-sa/ cc0/public-domain); -nc, -nd, and undeclared licenses get ``abstract=null`` plus an ``abstract_policy`` note with a link-out, and ``abstract_license`` carries the declared license. Null also where the publisher withholds the index entirely). Adds bulk — keep false for scans.",
            "default": false
          }
        }
      },
      "returns": "Returns ``{query, filters, sort, api_total, n_records_returned, records_truncated, records}``. ``api_total`` is OpenAlex's own total match count — ``records_truncated=true`` means more matches exist than were returned (tighten filters or raise max_records). Each record: ``{openalex_id, doi, pmid, title, publication_year/date, type, language, is_retracted, authors[{author_id, name, orcid, position, is_corresponding, institutions}], source{source_id, display_name, issn_l, type}, biblio, cited_by_count, fwci, referenced_works_count, open_access{is_oa, oa_status, oa_url}, best_oa_pdf_url, primary_topic, keywords}``.",
      "upstreams": [
        "openalex_works"
      ],
      "errorCases": [
        {
          "case": "openalex_key_required",
          "expectedError": {
            "error": "openalex_key_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "openalex_venue_info",
      "description": "Look up journals/repositories (\"sources\") in OpenAlex — OA status, DOAJ listing, APC, citation metrics.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "venue": {
            "type": "string",
            "description": "an exact identifier — OpenAlex S-id (``S106963461``), openalex.org URL, or ISSN (``1087-0156``) — for a single record; anything else is treated as a name search (e.g. ``\"Nature Biotechnology\"``)."
          },
          "max_records": {
            "type": "integer",
            "description": "cap for the name-search path (default 10, ceiling 500). Ignored for exact-identifier lookups.",
            "default": 10
          }
        },
        "required": [
          "venue"
        ]
      },
      "returns": "Returns: exact lookup -> one source record + ``counts_by_year``; name search -> ``{query, api_total, n_records_returned, records_truncated, records}``. Source record: ``{source_id, display_name, type (journal/repository/conference), issn_l, issn, host_organization, country_code, homepage_url, is_oa, is_in_doaj, is_core, apc_usd, works_count, cited_by_count, h_index, two_year_mean_citedness, first/last_publication_year, top_topics}``.",
      "upstreams": [
        "openalex_works"
      ],
      "errorCases": [
        {
          "case": "openalex_key_required",
          "expectedError": {
            "error": "openalex_key_required",
            "message": "string"
          }
        }
      ]
    }
  ],
  "upstreams": [
    {
      "package": "arxiv_fetch",
      "minIntervalSeconds": 3.0,
      "requestsPerSecond": 0.333
    },
    {
      "package": "openalex_works",
      "minIntervalSeconds": 0.5,
      "requestsPerSecond": 2.0
    }
  ]
}
