{
  "domain": "genes-ontologies",
  "package": "mcp_genes_ontologies",
  "derivedFrom": "bio-tools/lib/mcp_genes_ontologies/server.py",
  "toolCount": 10,
  "tools": [
    {
      "name": "get_go_annotations",
      "description": "Retrieve GO annotations for a UniProt gene product from QuickGO (complete, count-verified).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "uniprot_accession": {
            "type": "string",
            "description": "UniProt accession, e.g. \"P04637\" (prefix optional)."
          },
          "aspect": {
            "type": [
              "string",
              "null"
            ],
            "description": "None (all) or one of \"biological_process\", \"molecular_function\", \"cellular_component\"."
          },
          "evidence": {
            "type": [
              "string",
              "null"
            ],
            "description": "None (all), a preset (\"experimental_manual\" = manually-assigned experimental evidence, \"automatic_iea\" = electronic/IEA), or an explicit ECO code like \"ECO:0000314\". Three-letter GO evidence codes (IDA, IEA, ...) are NOT accepted — the QuickGO API silently ignores its goEvidence parameter, so filtering must use ECO codes."
          },
          "taxon_id": {
            "type": [
              "integer",
              "null"
            ],
            "description": "Optional NCBI taxon restriction (exact), e.g. 9606."
          },
          "include_term_names": {
            "type": "boolean",
            "description": "Hydrate each returned record with the GO term name, aspect and obsolete flag (one batched ontology lookup).",
            "default": true
          },
          "max_records": {
            "type": "integer",
            "description": "Cap on annotation records returned in `records` (the full set is still retrieved and summarized; `truncated` flags the cap).",
            "default": 500
          }
        },
        "required": [
          "uniprot_accession"
        ]
      },
      "returns": "Returns: {gene_product, total_annotations (API's own count), n_records, complete, truncated, distinct_go_ids (across ALL annotations, not just the returned page), records: [{go_id, go_aspect, qualifier, go_evidence, eco_id, reference, assigned_by, date, ...}]}.",
      "upstreams": [
        "quickgo_annotations"
      ]
    },
    {
      "name": "get_kegg_entries",
      "description": "Fetch KEGG entries by ID (genes, pathways, compounds, ...) — batched /get, 10 per request.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "KEGG entry IDs, e.g. [\"hsa:7157\"] (gene), [\"hsa04110\"] (pathway), [\"C00031\"] or [\"cpd:C00031\"] (compound). No duplicates. KEGG is rate-limited (academic REST service) — keep lists small."
          },
          "include_raw": {
            "type": "boolean",
            "description": "Also return the raw KEGG flat-file text per entry.",
            "default": false
          }
        },
        "required": [
          "ids"
        ]
      },
      "returns": "Returns: {n_entries, entries: [{requested_id, entry_id, record, raw?}]} where `record` is the structured parse: {entry, entry_id, entry_type, name, symbol, definition, organism, formula, pathway: [{id, name}], orthology: [{id, name}]}. An ID KEGG returns nothing for raises an error naming the missing IDs.",
      "upstreams": [
        "kegg_fetch"
      ]
    },
    {
      "name": "get_ontology_term",
      "description": "Fetch one ontology term's details, or its complete related-term set. With `relation=None`: full term record (label, synonyms, description, obsolete flag, direct parents). With a relation: the COMPLETE, fully paginated set of related terms — e.g. relation=\"hierarchicalChildren\" for direct children incl. part_of etc., \"descendants\"/\"hierarchicalDescendants\" for the whole subtree, \"ancestors\"/\"hierarchicalAncestors\", \"parents\", \"children\". Retrieval is count-verified against the API's own total.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "ontology": {
            "type": "string",
            "description": "OLS4 ontology ID (lowercase, e.g. \"efo\", \"go\", \"cl\", \"chebi\")."
          },
          "term_id": {
            "type": "string",
            "description": "Term CURIE (\"EFO:0000305\", \"GO:0006281\") or full IRI."
          },
          "relation": {
            "type": [
              "string",
              "null"
            ],
            "description": "None, or one of: parents, children, ancestors, descendants, hierarchicalParents, hierarchicalChildren, hierarchicalAncestors, hierarchicalDescendants."
          },
          "include_parents": {
            "type": "boolean",
            "description": "Include direct parent refs on the term record (only used when relation is None).",
            "default": true
          }
        },
        "required": [
          "ontology",
          "term_id"
        ]
      },
      "returns": "Returns: relation=None: {curie, iri, label, ontology, short_form, synonyms, description, is_obsolete, has_children, parents}. Otherwise: {root, relation, total_elements, term_count, terms: [...]}.",
      "upstreams": [
        "ols_terms"
      ]
    },
    {
      "name": "get_uniprot_entries",
      "description": "Fetch UniProtKB records for a list of accessions (batched OR-queries, not per-accession). Three modes: - `fields` given: token-lean tabular retrieval of just those UniProt fields (e.g. [\"accession\", \"id\", \"protein_name\", \"gene_names\", \"organism_name\", \"length\", \"sequence\"]); `format` is ignored. - format=\"fasta\": per-accession FASTA sequences. - format=\"txt\": per-accession full UniProt flat-file text (complete annotation; can be very large — prefer `fields` when you need specifics).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "accessions": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "UniProt accessions, e.g. [\"P04637\", \"P38398\"]."
          },
          "format": {
            "type": "string",
            "description": "\"fasta\" or \"txt\" (ignored when `fields` is given).",
            "default": "fasta"
          },
          "fields": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            },
            "description": "Optional UniProt REST field names for tabular mode."
          }
        },
        "required": [
          "accessions"
        ]
      },
      "returns": "Returns: fields mode: {accessions, fields, n_records, records: [{<column>: value}]}. fasta/txt mode: {accessions, format, n_found, missing, records: {accession: text}} — `missing` lists accessions UniProt returned no record for (merged/deleted accessions land there).",
      "upstreams": [
        "uniprot_fetch"
      ]
    },
    {
      "name": "link_kegg_ids",
      "description": "Cross-reference KEGG IDs against another database (/link) or convert to/from outside IDs (/conv). Examples: link [\"hsa:7157\"] to \"pathway\" (gene → pathways); conv [\"ncbi-geneid:7157\"] to \"hsa\" (NCBI Gene → KEGG gene) or [\"hsa:7157\"] to \"ncbi-geneid\"/\"uniprot\" (KEGG → outside IDs). Batched 10 IDs per request.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Source KEGG/outside IDs (prefixed, e.g. \"hsa:7157\", \"cpd:C00031\", \"ncbi-geneid:7157\")."
          },
          "target_db": {
            "type": "string",
            "description": "Target database (\"pathway\", \"ko\", \"enzyme\", \"hsa\", \"ncbi-geneid\", \"uniprot\", ...)."
          },
          "operation": {
            "type": "string",
            "description": "\"link\" (cross-reference within KEGG) or \"conv\" (convert between KEGG and outside identifier namespaces).",
            "default": "link"
          }
        },
        "required": [
          "ids",
          "target_db"
        ]
      },
      "returns": "Returns: {operation, target_db, query_ids, n_records, per_id_targets: {input_id: [target_ids]}, missing_ids (inputs with zero hits — reported explicitly, never silently dropped), records: [{source_id, source_db, target_id, target_db, ...}]}.",
      "upstreams": [
        "kegg_link"
      ]
    },
    {
      "name": "list_ontologies",
      "description": "List ontologies in the EBI Ontology Lookup Service (OLS4). With `ontology_ids` (e.g. [\"efo\", \"cl\", \"chebi\", \"go\", \"mondo\"]): fetch structured metadata records for just those ontologies; unknown IDs are reported in `not_found`. Without: the complete OLS4 catalogue (~250 ontologies, paginated fully and count-verified).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "ontology_ids": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            }
          }
        }
      },
      "returns": "Returns: {records: [{ontology_id, title, version, status, num_terms, ...}], not_found: [...]} for an ID list, or {records: [...], total_elements, complete} for the full catalogue.",
      "upstreams": [
        "ols_ontologies"
      ]
    },
    {
      "name": "map_reactome_pathways",
      "description": "Map gene symbols or UniProt accessions to Reactome pathways (AnalysisService token workflow).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "identifiers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Gene symbols (id_type=\"symbol\") or UniProt accessions (id_type=\"uniprot\"). No duplicates."
          },
          "id_type": {
            "type": "string",
            "description": "\"symbol\" or \"uniprot\".",
            "default": "symbol"
          },
          "species": {
            "type": "string",
            "description": "Species filter on returned pathways (default \"Homo sapiens\").",
            "default": "Homo sapiens"
          },
          "resource": {
            "type": "string",
            "description": "AnalysisService molecule-resource view (\"TOTAL\" default; \"UNIPROT\" restricts to protein-level mappings — the like-for-like view when comparing symbol vs accession submissions).",
            "default": "TOTAL"
          },
          "include_disease": {
            "type": "boolean",
            "description": "Include disease pathways (service default True).",
            "default": true
          },
          "compact": {
            "type": "boolean",
            "description": "True → per-identifier low-level pathways only ({stId, name, species}), plus the Reactome release version. False → the full deterministic result: per-identifier complete pathway sets with entity/reaction statistics (p-values, FDR, found/total counts) and the batch summary incl. identifiers_not_found.",
            "default": true
          }
        },
        "required": [
          "identifiers"
        ]
      },
      "returns": "Returns: compact: {tool, reactome_version, id_type, species, n_input, genes: {identifier: {found, n_lowlevel_pathways, pathways}}}. full: adds per-pathway statistics and batch_summary."
    },
    {
      "name": "query_genes",
      "description": "Resolve gene identifiers/symbols via mygene.info (batched, up to 1000 terms/request). Use this to map gene symbols to Ensembl gene IDs, Entrez IDs, names, and any other mygene.info field — or the reverse (set `scopes` to the namespace of your input terms, e.g. \"entrezgene\", \"ensembl.gene\", \"symbol,alias\").",
      "inputSchema": {
        "type": "object",
        "properties": {
          "terms": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Query terms (e.g. gene symbols [\"TP53\", \"BRCA1\"]). Terms containing commas are not supported."
          },
          "scopes": {
            "type": "string",
            "description": "Comma-separated identifier namespaces to match terms against (e.g. \"symbol\", \"entrezgene\", \"ensembl.gene\", \"symbol,alias\").",
            "default": "symbol"
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated mygene.info fields to return (e.g. \"symbol,name,taxid,entrezgene,ensembl.gene\"; \"all\" for everything).",
            "default": "symbol,name,taxid,entrezgene,ensembl.gene"
          },
          "species": {
            "type": "string",
            "description": "Species filter — common name (\"human\", \"mouse\") or NCBI taxid.",
            "default": "human"
          }
        },
        "required": [
          "terms"
        ]
      },
      "returns": "Returns: {n_input, n_records, not_found: [terms with no match], records: [...]}. A term matching several genes yields several records (each carries its `query` term). Records are deterministically ordered (input order, then _id).",
      "upstreams": [
        "mygene_query"
      ]
    },
    {
      "name": "search_kegg",
      "description": "Search a KEGG database by keyword (/find), with optional exact gene-symbol resolution. KEGG's /find matches the query as a SUBSTRING anywhere in the entry line — searching \"TP53\" in hsa returns TP53BP2, TP53I3, even alias-only matches, before TP53 itself. To resolve a gene symbol to its KEGG gene ID(s), set exact_gene_symbol=True: hits are filtered to rows whose symbol list contains the query exactly (case-insensitive); ambiguous symbols return ALL matching genes, zero matches is a normal outcome (not an error).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search text (gene symbol, compound name, keyword)."
          },
          "database": {
            "type": "string",
            "description": "KEGG database — an organism code (\"hsa\", \"mmu\") for genes, or \"compound\", \"pathway\", \"drug\", ...",
            "default": "hsa"
          },
          "exact_gene_symbol": {
            "type": "boolean",
            "description": "Apply exact symbol-list filtering (gene databases).",
            "default": false
          },
          "max_hits": {
            "type": "integer",
            "description": "Cap on raw keyword hits returned (ignored in exact mode).",
            "default": 200
          }
        },
        "required": [
          "query"
        ]
      },
      "returns": "Returns: exact mode: {symbol, organism, n_matches, matches: [{entry_id, symbols, description}]}. keyword mode: {query, database, total_hits, n_returned, truncated, hits: [{entry_id, description}]}.",
      "upstreams": [
        "kegg_link"
      ]
    },
    {
      "name": "search_ontology_terms",
      "description": "Search ontology terms by label/synonym across one or more OLS4 ontologies. Typical uses: find an EFO ID for a disease name (ontologies=[\"efo\"]), Cell Ontology terms for a cell type ([\"cl\"]), ChEBI terms for a chemical ([\"chebi\"]), GO terms by name ([\"go\"]) — or search all ontologies at once.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search text (term label, synonym, or identifier)."
          },
          "ontologies": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            },
            "description": "Ontology IDs to restrict the search to (lowercase, e.g. [\"efo\"], [\"cl\", \"uberon\"]); None searches every ontology."
          },
          "exact": {
            "type": "boolean",
            "description": "Require exact (whole-string) label/synonym match.",
            "default": false
          },
          "include_obsolete": {
            "type": "boolean",
            "description": "Also return obsolete terms (default False).",
            "default": false
          },
          "max_results": {
            "type": "integer",
            "description": "Maximum terms returned (ranked by OLS relevance).",
            "default": 25
          }
        },
        "required": [
          "query"
        ]
      },
      "returns": "Returns: {query, total_found, n_returned, truncated, terms: [{curie, iri, label, short_form, ontology, description, type, is_defining_ontology}]}.",
      "upstreams": [
        "ols_terms"
      ]
    }
  ],
  "upstreams": [
    {
      "package": "kegg_fetch",
      "minIntervalSeconds": 0.35,
      "requestsPerSecond": 2.857
    },
    {
      "package": "kegg_link",
      "minIntervalSeconds": 0.35,
      "requestsPerSecond": 2.857
    },
    {
      "package": "ols_ontologies",
      "minIntervalSeconds": 0.5,
      "requestsPerSecond": 2.0
    },
    {
      "package": "ols_terms",
      "minIntervalSeconds": 0.5,
      "requestsPerSecond": 2.0
    },
    {
      "package": "quickgo_annotations",
      "minIntervalSeconds": 0.5,
      "requestsPerSecond": 2.0
    }
  ]
}
