{
  "domain": "drug-regulatory",
  "package": "mcp_drug_regulatory",
  "derivedFrom": "bio-tools/lib/mcp_drug_regulatory/server.py",
  "toolCount": 7,
  "tools": [
    {
      "name": "count_drug_applications",
      "description": "Aggregate Drugs@FDA applications: bucket counts over one field, optionally filtered.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "count_field": {
            "type": "string",
            "description": "One of the verified friendly names — \"sponsor_name\", \"application_number\", \"dosage_form\", \"route\", \"marketing_status\", \"te_code\", \"pharm_class_epc\", \"pharm_class_moa\", \"pharm_class_cs\", \"pharm_class_pe\" — or a raw openFDA field path (\".exact\" suffix required for analyzed fields). brand/generic/.../pharm_class: Optional filters (same semantics as search_drug_applications); no filters = aggregate the whole corpus."
          },
          "brand": {
            "type": [
              "string",
              "null"
            ]
          },
          "generic": {
            "type": [
              "string",
              "null"
            ]
          },
          "active_ingredient": {
            "type": [
              "string",
              "null"
            ]
          },
          "sponsor": {
            "type": [
              "string",
              "null"
            ]
          },
          "marketing_status": {
            "type": [
              "string",
              "null"
            ]
          },
          "dosage_form": {
            "type": [
              "string",
              "null"
            ]
          },
          "route": {
            "type": [
              "string",
              "null"
            ]
          },
          "pharm_class": {
            "type": [
              "string",
              "null"
            ]
          },
          "pharm_class_type": {
            "type": "string",
            "default": "epc"
          },
          "search_type": {
            "type": "string",
            "default": "and"
          },
          "max_buckets": {
            "type": "integer",
            "description": "Maximum buckets (openFDA cap: 1000).",
            "default": 1000
          }
        },
        "required": [
          "count_field"
        ]
      },
      "returns": "Returns: {count_field, api_field, n_buckets, bucket_sum, buckets: [{term, count}, ...] in descending count order}.",
      "upstreams": [
        "openfda_drugsfda"
      ]
    },
    {
      "name": "get_drug_application",
      "description": "Fetch one Drugs@FDA application by its number (e.g. \"NDA020702\", \"ANDA076543\", \"BLA125514\").",
      "inputSchema": {
        "type": "object",
        "properties": {
          "application_number": {
            "type": "string"
          }
        },
        "required": [
          "application_number"
        ]
      },
      "returns": "Returns: {application_number, found, record} — `record` carries sponsor_name, products (brand name, active ingredients + strengths, dosage form, route, marketing status, TE code), the full regulatory submission history, and harmonized openfda fields (generic/brand names, UNII, RxCUI, NDCs, pharmacologic classes) when present; `record` is null and `found` false if the number does not exist.",
      "upstreams": [
        "openfda_drugsfda"
      ]
    },
    {
      "name": "get_drug_statistics",
      "description": "Corpus-level Drugs@FDA statistics in one call.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      },
      "returns": "Returns: {total_applications, last_updated, marketing_status: [{term, count}], dosage_form_top (top 25), dosage_form_distinct, route_top (top 25), route_distinct, sponsor_top (top 25 by application count)}.",
      "upstreams": [
        "openfda_drugsfda"
      ]
    },
    {
      "name": "get_generic_equivalents",
      "description": "Find applications sharing a brand-name drug's exact active-ingredient set. Resolves the brand to its reference application(s), extracts the exact active-ingredient name set(s), then returns every Drugs@FDA application (brand or generic, any sponsor) whose product ingredient set matches.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "brand": {
            "type": "string"
          }
        },
        "required": [
          "brand"
        ]
      },
      "returns": "Returns: {brand, reference_applications: [application_numbers], active_ingredient_sets: [[names]], equivalents: [full application records incl. TE codes and marketing status]}.",
      "upstreams": [
        "openfda_drugsfda"
      ]
    },
    {
      "name": "list_pharmacologic_classes",
      "description": "Enumerate pharmacologic classes present in Drugs@FDA, with application counts.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "class_type": {
            "type": "string",
            "description": "\"epc\" (Established Pharmacologic Class, default), \"moa\" (Mechanism of Action), \"cs\" (Chemical Structure), or \"pe\" (Physiologic Effect).",
            "default": "epc"
          },
          "max_buckets": {
            "type": "integer",
            "description": "Maximum classes returned (openFDA cap: 1000).",
            "default": 1000
          }
        }
      },
      "returns": "Returns: {class_type, n_classes, classes: [{term, count}, ...] in descending count order}. Counts reflect only applications with a harmonized openfda block (older records lack one).",
      "upstreams": [
        "openfda_drugsfda"
      ]
    },
    {
      "name": "search_drug_applications",
      "description": "Search FDA Drugs@FDA applications (NDA/ANDA/BLA) by any combination of filters. All text filters are exact-phrase matches. Use this for multi-field regulatory search: by brand/generic name, active ingredient, sponsor, marketing status (\"Prescription\", \"Over-the-counter\", \"Discontinued\"), dosage form, route, or pharmacologic class (therapeutic-class search: pharm_class + pharm_class_type \"epc\" (Established Pharmacologic Class) | \"moa\" (Mechanism of Action) | \"cs\" (Chemical Structure) | \"pe\" (Physiologic Effect)). Caveat: `generic` and `pharm_class` query the harmonized openfda block, which is absent on many older applications — those records are silently skipped by upstream openFDA. Retrieval is complete and count-verified; result sets over 26,000 applications raise with guidance (narrow with submission_date_from/to).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "brand": {
            "type": [
              "string",
              "null"
            ]
          },
          "generic": {
            "type": [
              "string",
              "null"
            ]
          },
          "active_ingredient": {
            "type": [
              "string",
              "null"
            ]
          },
          "sponsor": {
            "type": [
              "string",
              "null"
            ]
          },
          "marketing_status": {
            "type": [
              "string",
              "null"
            ]
          },
          "dosage_form": {
            "type": [
              "string",
              "null"
            ]
          },
          "route": {
            "type": [
              "string",
              "null"
            ]
          },
          "pharm_class": {
            "type": [
              "string",
              "null"
            ]
          },
          "pharm_class_type": {
            "type": "string",
            "default": "epc"
          },
          "search_type": {
            "type": "string",
            "description": "\"and\" (default) or \"or\" — how the provided filters combine. submission_date_from/to: Constrain submissions.submission_status_date (YYYY-MM-DD); always ANDed.",
            "default": "and"
          },
          "submission_date_from": {
            "type": [
              "string",
              "null"
            ]
          },
          "submission_date_to": {
            "type": [
              "string",
              "null"
            ]
          },
          "raw_search": {
            "type": [
              "string",
              "null"
            ],
            "description": "Verbatim openFDA search= string (escape hatch; overrides the mapped filters)."
          },
          "max_records": {
            "type": "integer",
            "description": "Cap on application records returned (full set is still retrieved and counted; `truncated` flags the cap).",
            "default": 100
          }
        }
      },
      "returns": "Returns: {total (API's own count), n_returned, truncated, last_updated, records: [{application_number, sponsor_name, products: [...], submissions: [...], openfda_generic_name, openfda_pharm_class_epc, ...}]}.",
      "upstreams": [
        "openfda_drugsfda"
      ]
    },
    {
      "name": "search_drug_labels",
      "description": "Retrieve FDA drug product labels (SPL) by ingredient/name/route, with targeted section extraction. Default records carry identification (set_id, SPL version, effective date, brand/generic/substance names, manufacturer, route, product type, application numbers), boxed-warning presence, which warning-type sections exist, and the indications_and_usage text. Pass `sections` to instead get just those raw label sections per record (token-lean) — e.g. [\"boxed_warning\"], [\"dosage_and_administration\", \"contraindications\"], [\"adverse_reactions\"]. Matching: analyzed fields are tokenized — brand_name \"Tylenol\" also matches \"Tylenol Extra Strength\"; active_ingredient \"FENTANYL\" also matches \"FENTANYL CITRATE\". Set exact=true for whole-string matching.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "active_ingredient": {
            "type": [
              "string",
              "null"
            ],
            "description": "Substance name (openfda.substance_name). generic_name / brand_name / route: openfda label fields."
          },
          "generic_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "brand_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "route": {
            "type": [
              "string",
              "null"
            ]
          },
          "product_type": {
            "type": [
              "string",
              "null"
            ],
            "description": "\"HUMAN PRESCRIPTION DRUG\" or \"HUMAN OTC DRUG\"."
          },
          "exact": {
            "type": "boolean",
            "description": "Query the non-analyzed .exact field variants.",
            "default": false
          },
          "raw_search": {
            "type": [
              "string",
              "null"
            ],
            "description": "Verbatim openFDA search= string (escape hatch; mutually exclusive with the mapped filters)."
          },
          "sections": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            },
            "description": "Raw openFDA label section names to extract instead of the default structured record."
          },
          "max_records": {
            "type": "integer",
            "description": "Cap on label records returned (retrieval itself is complete and count-verified; `truncated` flags the cap).",
            "default": 50
          }
        }
      },
      "returns": "Returns: {search, total (API count), n_returned, truncated, records: [...]}.",
      "upstreams": [
        "openfda_labels"
      ]
    }
  ]
}
