{
  "domain": "variants",
  "package": "mcp_variants",
  "derivedFrom": "bio-tools/lib/mcp_variants/server.py",
  "toolCount": 18,
  "tools": [
    {
      "name": "cadd_position_scores",
      "description": "CADD scores for ALL possible single-nucleotide substitutions at one genomic position (up to 3 records, one per alt allele).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "chrom": {
            "type": "string",
            "description": "chromosome ``1``-``22``, ``X`` or ``Y``."
          },
          "pos": {
            "type": "integer",
            "description": "1-based position on the build embedded in ``version``."
          },
          "version": {
            "type": "string",
            "description": "build-pinned CADD release (default ``GRCh38-v1.7``; see ``cadd_variant_score``)."
          }
        },
        "required": [
          "chrom",
          "pos"
        ]
      },
      "returns": "Returns ``{query: {type, version, chrom, pos}, records: [{chrom, pos, ref, alt, raw_score, phred}, ...]}`` sorted by (chrom, pos, ref, alt). Raises an error if the position has no scored rows.",
      "upstreams": [
        "cadd_scores"
      ]
    },
    {
      "name": "cadd_range_scores",
      "description": "CADD scores for every SNV in a genomic window [start, end] (inclusive, max 100 bp — split larger spans into consecutive windows). One request scores the whole window; far cheaper than per-position calls.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "chrom": {
            "type": "string",
            "description": "chromosome ``1``-``22``, ``X`` or ``Y``."
          },
          "start": {
            "type": "integer",
            "description": "window start (1-based, inclusive)."
          },
          "end": {
            "type": "integer",
            "description": "window end (inclusive); ``end - start + 1`` <= 100."
          },
          "version": {
            "type": "string",
            "description": "build-pinned CADD release (default ``GRCh38-v1.7``; see ``cadd_variant_score``)."
          }
        },
        "required": [
          "chrom",
          "start",
          "end"
        ]
      },
      "returns": "Returns ``{query: {type, version, chrom, start, end, span_bp}, n_records, n_positions_scored, records}`` with records sorted by (chrom, pos, ref, alt).",
      "upstreams": [
        "cadd_scores"
      ]
    },
    {
      "name": "cadd_variant_score",
      "description": "CADD deleteriousness score for one SNV (ref>alt at chrom:pos). Higher PHRED = more likely deleterious (>=20 ~ top 1% of the genome). The reference allele is verified against ``ref`` — wrong-build coordinates fail loudly instead of returning a real-looking score for the wrong locus.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "chrom": {
            "type": "string",
            "description": "chromosome ``1``-``22``, ``X`` or ``Y`` (``chr`` prefix tolerated; CADD scores nuclear SNVs only)."
          },
          "pos": {
            "type": "integer",
            "description": "1-based position on the build embedded in ``version``."
          },
          "ref": {
            "type": "string",
            "description": "reference allele (A/C/G/T) — must match the genome at pos."
          },
          "alt": {
            "type": "string",
            "description": "alternate allele (A/C/G/T), different from ref."
          },
          "version": {
            "type": "string",
            "description": "CADD release pinned WITH its genome build, e.g. ``GRCh38-v1.7`` (default), ``GRCh37-v1.7``, ``GRCh38-v1.6``, ``GRCh37-v1.6``. A bare ``v1.7`` is rejected here because upstream silently returns no rows for it."
          }
        },
        "required": [
          "chrom",
          "pos",
          "ref",
          "alt"
        ]
      },
      "returns": "Returns ``{query: {type, version, chrom, pos, ref, alt}, record: {chrom, pos, ref, alt, raw_score, phred}}``. ``raw_score``/``phred`` are the API's exact decimal strings. Errors: reference-allele mismatch (wrong build/typo), no row for alt, or empty result (position outside the scored genome).",
      "upstreams": [
        "cadd_scores"
      ]
    },
    {
      "name": "clinvar_get_records",
      "description": "Fetch full ClinVar records for a batch of VCV/RCV accessions or bare variation IDs. Requires a contact email (Settings → Privacy → 'Share contact email with research data services') per NCBI E-utilities usage policy.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "accessions": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "up to 50 identifiers, mixed forms accepted — ``VCV000045122`` (versioned ``VCV000045122.3`` ok; resolved locally, free), ``RCV000019428`` (each RCV costs one extra lookup request), or a bare ClinVar variation ID (``45122``). rsIDs are rejected — use ``clinvar_variant_by_rsid``. Note an RCV (one variant-condition pair) resolves to its parent VCV variation record."
          }
        },
        "required": [
          "accessions"
        ]
      },
      "returns": "Returns ``{n_requested, records, not_found, missing_uids, not_processed}``. Records carry the full shape documented in ``clinvar_search`` plus ``requested_as`` (which input(s) mapped to the record), sorted by variation_id. ``not_found`` lists unknown accessions (definitive absence — RCVs that esearch proves unknown); ``missing_uids`` lists inputs whose summary document NCBI dropped or error-flagged: for an RCV that just resolved via esearch this is a transient drop (the record EXISTS — retry); for a VCV/numeric input it is either a transient drop or a nonexistent id — retry to disambiguate, never conclude absence from one call; ``not_processed`` lists RCVs skipped because the per-call time budget ran out (re-request just those — VCV/numeric inputs always resolve, they never land there). Never silently drops an input.",
      "upstreams": [
        "clinvar_records"
      ],
      "errorCases": [
        {
          "case": "contact_email_required",
          "expectedError": {
            "error": "contact_email_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "clinvar_search",
      "description": "Search ClinVar directly (live NCBI, not gnomAD's snapshot) and return matching variation records with clinical significance, review status and gold stars. Requires a contact email (Settings → Privacy → 'Share contact email with research data services') per NCBI E-utilities usage policy.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "ClinVar Entrez query. Free text works (``\"TP53 R175H\"``, a condition name, an HGVS string), and fielded terms compose with AND/OR/NOT — useful fields: ``BRCA1[gene]``, ``pathogenic[CLIN_SIG]`` (also ``likely_pathogenic``, ``uncertain_significance``, ``benign``, ``conflicting...``), ``\"Lynch syndrome\"[dis]``, ``single_nucleotide_variant[Type of variation]``, ``clinsig_has_assertion[PROP]``. An rsID (``rs121913529``) also works, but ``clinvar_variant_by_rsid`` returns fuller records for that case."
          },
          "max_records": {
            "type": "integer",
            "description": "page cap, 1-200 (default 50). The match TOTAL is always reported; when total > max_records the list is a capped prefix and ``truncated`` is true (results come in ClinVar's default relevance/recency order — narrow the query to see the rest).",
            "default": 50
          }
        },
        "required": [
          "query"
        ]
      },
      "returns": "Returns ``{term, total, n_returned, truncated, missing_uids, records}``. ``missing_uids`` lists any matched IDs whose summary document NCBI failed to return (rare, transient — distinct from truncation; retry to recover them). Each record: ``{variation_id, accession (VCV), accession_version, title, obj_type, variant_type, canonical_spdi, cdna_change, protein_change, rsids, other_xrefs, genes, molecular_consequences, locations (GRCh38+GRCh37 coordinates), allele_frequencies, germline_classification, clinical_impact_classification, oncogenicity_classification (each: description, review_status, gold_stars 0-4, last_evaluated, conditions with ontology xrefs; null when ClinVar has no classification on that axis), n_submissions (SCV count), supporting_submissions}``. Quirk: NCBI E-utilities intermittently returns HTTP 500 under load — retry once more a few seconds later if that surfaces.",
      "upstreams": [
        "clinvar_records"
      ],
      "errorCases": [
        {
          "case": "contact_email_required",
          "expectedError": {
            "error": "contact_email_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "clinvar_variant_by_rsid",
      "description": "All ClinVar variation records that reference a dbSNP rsID, with full classifications (an rsID can map to several VCVs — one per alternate allele, e.g. rs121913529 covers KRAS G12D/G12V/G12A). Requires a contact email (Settings → Privacy → 'Share contact email with research data services') per NCBI E-utilities usage policy.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rsid": {
            "type": "string",
            "description": "dbSNP reference SNP ID, e.g. ``rs7412`` (case-insensitive; must match ``rs<digits>``)."
          },
          "max_records": {
            "type": "integer",
            "description": "cap, 1-200 (default 50); ``total`` always carries the true match count and ``truncated`` flags a capped listing.",
            "default": 50
          }
        },
        "required": [
          "rsid"
        ]
      },
      "returns": "Returns ``{rsid, total, n_returned, truncated, missing_uids, records}`` with the full record shape documented in ``clinvar_search`` (review status, gold stars, last-evaluated dates, SCV counts — the fields gnomAD's ClinVar mirror lacks). Records come in ClinVar's relevance order; ``missing_uids`` lists matches whose summary NCBI dropped (transient). ``total == 0`` means ClinVar has no record for the rsID.",
      "upstreams": [
        "clinvar_records"
      ],
      "errorCases": [
        {
          "case": "contact_email_required",
          "expectedError": {
            "error": "contact_email_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "clinvar_variants",
      "description": "List ClinVar variants in a gene as mirrored by gnomAD, with clinical significance, review status and gold stars. The output pins gnomAD's ClinVar snapshot via ``clinvar_release_date``.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gene_symbol": {
            "type": [
              "string",
              "null"
            ],
            "description": "HGNC symbol (e.g. ``BRCA1``). Pass exactly one of gene_symbol / gene_id."
          },
          "gene_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Ensembl gene ID."
          }
        }
      },
      "returns": "Returns ``{gene_id, symbol, clinvar_release_date, n_variants, variants}``; each row: ``{variant_id, clinvar_variation_id, clinical_significance, gold_stars, review_status, major_consequence, pos, transcript_id, in_gnomad}``.",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "dbsnp_get_rsids",
      "description": "Canonical dbSNP RefSNP records for a batch of rsIDs: GRCh38+GRCh37 placements, alleles, gene context, per-study allele frequencies, and ClinVar cross-references. Requires a contact email (Settings → Privacy → 'Share contact email with research data services') per NCBI E-utilities usage policy.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rsids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "up to 20 rsIDs (``rs<digits>``, case-insensitive). Each costs one paced NCBI Variation Services request, so large batches take ~1 s per rsID."
          }
        },
        "required": [
          "rsids"
        ]
      },
      "returns": "Returns ``{n_requested, records, not_found, not_processed}``. ``not_found`` lists rs numbers dbSNP doesn't know; ``not_processed`` lists rsIDs skipped when the per-call time budget ran out (re-request just those). Each record: ``{rsid, status, create_date, last_update_date, last_update_build_id, n_citations, citations_pmids (capped at 20, ``citations_truncated`` flags the cap), variant_type, mane_select_ids, placements, alleles}``. ``status`` is ``live``, ``merged`` (record instead carries ``merged_into`` — re-query those rsIDs), or ``no_data`` (withdrawn/unsupported). ``placements`` give 1-based chromosome coordinates with ref/alts per assembly (GRCh38 first, ``is_primary`` true). Each alt-allele entry: ``{allele, ref, spdi (0-based interbase), hgvs, frequencies: [{study, study_version, allele_count, total_count, af}] (ALFA, 1000Genomes, TOPMED, gnomAD...), clinvar: [{rcv_accession, clinical_significances, review_status, last_evaluated_date, disease_names}], genes: [{symbol, gene_id, name, orientation, consequences (SO terms), mane_select: [{transcript_hgvs, protein_spdi}]}]}``.",
      "upstreams": [
        "dbsnp_records"
      ],
      "errorCases": [
        {
          "case": "contact_email_required",
          "expectedError": {
            "error": "contact_email_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "dbsnp_search_by_region",
      "description": "List dbSNP rsIDs in a genomic window (esearch db=snp positional index — NCBI Variation Services has no region endpoint). Requires a contact email (Settings → Privacy → 'Share contact email with research data services') per NCBI E-utilities usage policy.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "chrom": {
            "type": "string",
            "description": "chromosome ``1``-``22``, ``X``, ``Y`` or ``MT`` (``chr`` prefix tolerated)."
          },
          "start": {
            "type": "integer",
            "description": "window start, 1-based inclusive."
          },
          "stop": {
            "type": "integer",
            "description": "window end, inclusive; span capped at 1 Mb (split larger regions into consecutive windows). Dense regions hold many thousands of rsIDs per kb — keep windows small or raise max_rsids."
          },
          "assembly": {
            "type": "string",
            "description": "which positional index to query — ``GRCh38`` (default) or ``GRCh37``. Coordinates must be on the chosen assembly.",
            "default": "GRCh38"
          },
          "max_rsids": {
            "type": "integer",
            "description": "listing cap, 1-1000 (default 200).",
            "default": 200
          }
        },
        "required": [
          "chrom",
          "start",
          "stop"
        ]
      },
      "returns": "Returns ``{chrom, start, stop, assembly, term (the exact Entrez query used), total (the API's own count), n_returned, truncated, rsids}``. ``truncated`` is true when total > n_returned — the list is then a prefix in Entrez default order (descending rs number), never a silent truncation. Feed rsIDs (<= 20 at a time) to ``dbsnp_get_rsids`` for full records.",
      "upstreams": [
        "dbsnp_records"
      ],
      "errorCases": [
        {
          "case": "contact_email_required",
          "expectedError": {
            "error": "contact_email_required",
            "message": "string"
          }
        }
      ]
    },
    {
      "name": "gene_constraint",
      "description": "gnomAD gene constraint metrics: pLI, observed/expected LoF-missense- synonymous counts with oe ratios + 90% CI bounds, and per-class z-scores. Use to judge a gene's intolerance to loss-of-function (pLI >= 0.9 or oe_lof_upper (LOEUF) < 0.6 ~ LoF-intolerant).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gene_symbol": {
            "type": [
              "string",
              "null"
            ],
            "description": "HGNC symbol (e.g. ``TP53``). Pass exactly one of gene_symbol / gene_id."
          },
          "gene_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Ensembl gene ID."
          }
        }
      },
      "returns": "Returns ``{gene_id, symbol, canonical_transcript_id, chrom, start, stop, strand, constraint: {exp_lof, obs_lof, oe_lof, oe_lof_lower, oe_lof_upper, exp_mis, obs_mis, oe_mis, oe_mis_lower, oe_mis_upper, exp_syn, obs_syn, oe_syn, oe_syn_lower, oe_syn_upper, pli, lof_z, mis_z, syn_z}}``.",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "gene_variants",
      "description": "List ALL gnomAD short variants in a gene (complete listing — can be thousands of rows for large genes).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gene_symbol": {
            "type": [
              "string",
              "null"
            ],
            "description": "HGNC symbol (e.g. ``APOE``). Pass exactly one of gene_symbol / gene_id."
          },
          "gene_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Ensembl gene ID (e.g. ``ENSG00000130203``)."
          },
          "dataset": {
            "type": "string",
            "description": "gnomAD dataset pin (default ``gnomad_r4``; see ``get_variant``)."
          }
        }
      },
      "returns": "Returns ``{gene_id, symbol, chrom, start, stop, dataset, n_variants, variants}``; each variant row has ``{variant_id, pos, ref, alt, rsids, exome, genome}`` frequency blocks, sorted by (pos, variant_id).",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "get_structural_variant",
      "description": "Look up one gnomAD structural variant by its release-specific SV ID.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sv_id": {
            "type": "string",
            "description": "SV ID as listed by ``structural_variants`` (e.g. ``DEL_chr17_599b1512`` in gnomad_sv_r4). IDs do NOT carry across releases."
          },
          "dataset": {
            "type": "string",
            "description": "SV dataset pin — ``gnomad_sv_r4`` (default) or ``gnomad_sv_r2_1``. Must match the release the ID came from."
          }
        },
        "required": [
          "sv_id"
        ]
      },
      "returns": "Returns ``{found, sv_id, dataset, structural_variant}`` where ``structural_variant`` is null when not found.",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "get_variant",
      "description": "Look up one gnomAD short variant by ID and return its population frequencies.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "variant_id": {
            "type": "string",
            "description": "gnomAD variant ID ``chrom-pos-ref-alt`` on the dataset's reference build (GRCh38 for r3/r4, GRCh37 for r2.1/ExAC), e.g. ``19-44908822-C-T`` (APOE rs7412). Use ``search_variants`` to resolve an rsID first."
          },
          "dataset": {
            "type": "string",
            "description": "gnomAD dataset pin. Default ``gnomad_r4`` (the current release, exomes+genomes). Other values: ``gnomad_r4_non_ukb``, ``gnomad_r3`` (+ ``_controls_and_biobanks``/``_non_cancer``/ ``_non_neuro``/``_non_topmed``/``_non_v2`` subsets; genome-only), ``gnomad_r2_1`` (+ subsets; GRCh37), ``exac``."
          }
        },
        "required": [
          "variant_id"
        ]
      },
      "returns": "Returns a dict: ``found`` (bool), ``variant_id``, ``dataset`` (the pin actually queried), and ``variant`` — null when not found, else ``{variant_id, dataset, reference_genome, chrom, pos, ref, alt, rsids, exome: {ac, an, af, homozygote_count, hemizygote_count, filters}, genome: {...same keys}}``. ``exome``/``genome`` are null where the dataset has no such call set (e.g. r3 is genome-only).",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "liftover_variant",
      "description": "Map a variant ID between reference builds (GRCh37 <-> GRCh38) using gnomAD's liftover table.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "variant_id": {
            "type": "string",
            "description": "source variant ID ``chrom-pos-ref-alt`` on ``source_build``."
          },
          "source_build": {
            "type": "string",
            "description": "build of the input ID — ``GRCh37`` (default) or ``GRCh38``. The route is directional: a GRCh38 ID with ``source_build=GRCh37`` returns zero results, not an error.",
            "default": "GRCh37"
          }
        },
        "required": [
          "variant_id"
        ]
      },
      "returns": "Returns ``{source_variant_id, source_build, n_results, results}``; each result is ``{source: {variant_id, reference_genome}, liftover: {variant_id, reference_genome}, datasets}``.",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "mitochondrial_variants",
      "description": "List gnomAD mitochondrial variants with heteroplasmy-aware counts (``ac_het``, ``ac_hom``, ``max_heteroplasmy``) for a mitochondrial gene OR a chrM coordinate window.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gene_symbol": {
            "type": [
              "string",
              "null"
            ],
            "description": "mitochondrial gene symbol (e.g. ``MT-TL1``). Pass a gene (symbol or ID) OR a region, not both."
          },
          "gene_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Ensembl gene ID of a mitochondrial gene."
          },
          "region_start": {
            "type": [
              "integer",
              "null"
            ],
            "description": "chrM window start (pass together with region_stop)."
          },
          "region_stop": {
            "type": [
              "integer",
              "null"
            ],
            "description": "chrM window end (inclusive)."
          },
          "dataset": {
            "type": "string",
            "description": "gnomAD dataset pin (default ``gnomad_r4``)."
          }
        }
      },
      "returns": "Returns ``{gene_id+symbol | region, dataset, n_variants, variants}``; each row: ``{variant_id, pos, ac_het, ac_hom, an, max_heteroplasmy, filters}``.",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "region_variants",
      "description": "List ALL gnomAD short variants in a genomic region (max 1 Mb — split larger regions into consecutive windows).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "chrom": {
            "type": "string",
            "description": "chromosome name without ``chr`` prefix (``1``-``22``, ``X``, ``Y``; use ``M`` region queries via ``mitochondrial_variants``)."
          },
          "start": {
            "type": "integer",
            "description": "region start (1-based, inclusive)."
          },
          "stop": {
            "type": "integer",
            "description": "region end (inclusive). ``stop - start`` must be <= 1,000,000."
          },
          "dataset": {
            "type": "string",
            "description": "gnomAD dataset pin (default ``gnomad_r4``; determines the reference build of the coordinates — GRCh38 for r3/r4)."
          }
        },
        "required": [
          "chrom",
          "start",
          "stop"
        ]
      },
      "returns": "Returns ``{chrom, start, stop, dataset, n_variants, variants}`` with the same lean variant rows as ``gene_variants``.",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "search_variants",
      "description": "Search gnomAD for variant IDs matching a query string (rsID like ``rs7412``, a variant ID, or a prefix). Use this to resolve rsIDs to ``chrom-pos-ref-alt`` IDs for ``get_variant``.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "search string (rsID or variant-ID text)."
          },
          "dataset": {
            "type": "string",
            "description": "gnomAD dataset pin (default ``gnomad_r4``; see ``get_variant`` for the full enum)."
          }
        },
        "required": [
          "query"
        ]
      },
      "returns": "Returns ``{query, dataset, n_matches, variant_ids}`` with ``variant_ids`` sorted.",
      "upstreams": [
        "gnomad_variants"
      ]
    },
    {
      "name": "structural_variants",
      "description": "List gnomAD structural variants (deletions, duplications, insertions, inversions, CNVs...) overlapping a gene.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gene_symbol": {
            "type": [
              "string",
              "null"
            ],
            "description": "HGNC symbol (e.g. ``TP53``). Pass exactly one of gene_symbol / gene_id."
          },
          "gene_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Ensembl gene ID."
          },
          "dataset": {
            "type": "string",
            "description": "SV dataset pin — ``gnomad_sv_r4`` (default, GRCh38) or ``gnomad_sv_r2_1`` (GRCh37). SV IDs are release-specific."
          }
        }
      },
      "returns": "Returns ``{gene_id, symbol, dataset, n_variants, variants}``; rows carry SV ID, type, position/length, allele counts/frequencies, filters, per-gene consequences, and calling algorithms/evidence where present.",
      "upstreams": [
        "gnomad_variants"
      ]
    }
  ],
  "upstreams": [
    {
      "package": "cadd_scores",
      "minIntervalSeconds": 0.5,
      "requestsPerSecond": 2.0
    },
    {
      "package": "clinvar_records",
      "minIntervalSeconds": 0.4,
      "requestsPerSecond": 2.5
    },
    {
      "package": "dbsnp_records",
      "minIntervalSeconds": 0.4,
      "requestsPerSecond": 2.5
    },
    {
      "package": "gnomad_variants",
      "minIntervalSeconds": 6.0,
      "requestsPerSecond": 0.167
    }
  ]
}
