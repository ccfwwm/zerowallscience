{
  "domain": "expression",
  "package": "mcp_expression",
  "derivedFrom": "bio-tools/lib/mcp_expression/server.py",
  "toolCount": 15,
  "tools": [
    {
      "name": "gtex_calculate_eqtl",
      "description": "Calculate an eQTL on the fly for any gene-variant pair in one tissue. Unlike gtex_single_tissue_eqtls (precomputed significant associations only), this computes the association dynamically — including non-significant pairs. Returns p-value, NES, t-statistic, MAF and the per-sample genotype/expression arrays (deterministically sorted by (genotype, expression); upstream returns them in random order).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gencode_id": {
            "type": "string"
          },
          "variant_id": {
            "type": "string"
          },
          "tissue_site_detail_id": {
            "type": "string"
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        },
        "required": [
          "gencode_id",
          "variant_id",
          "tissue_site_detail_id"
        ]
      }
    },
    {
      "name": "gtex_dataset_info",
      "description": "List all GTEx dataset releases with their metadata.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      },
      "returns": "Returns every dataset release (datasetId, GENCODE version, genome build, dbSNP build, sample/subject/tissue counts). Use this to choose a dataset_id to pin for the other gtex_* tools and to know which GENCODE version its gene ids come from."
    },
    {
      "name": "gtex_eqtl_genes",
      "description": "Get all eGenes (genes with >=1 significant cis-eQTL) for a tissue. The complete eGene list is walked page-by-page and count-verified (e.g. Pancreas in gtex_v8 has 9,660 eGenes); `total` is the true count. Rows include empirical p-values, q-values and log2 allelic fold change. Set max_genes to cap the returned rows (`truncated` flags the cap; `total` stays exact).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "tissue_site_detail_id": {
            "type": "string"
          },
          "max_genes": {
            "type": [
              "integer",
              "null"
            ]
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        },
        "required": [
          "tissue_site_detail_id"
        ]
      }
    },
    {
      "name": "gtex_expression_summary",
      "description": "Summarize a gene's expression across ALL GTEx tissues, ranked by median TPM. Accepts a gene symbol ('GAPDH') or Ensembl id; the symbol is resolved to the pinned dataset's versioned GENCODE id automatically. Returns the resolved gene record plus every tissue ranked by descending median TPM — the quickest way to profile baseline tissue specificity of a target. Errors if the symbol is not in the GTEx reference.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gene": {
            "type": "string"
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        },
        "required": [
          "gene"
        ]
      }
    },
    {
      "name": "gtex_gene_expression",
      "description": "Get sample-level (not aggregated) expression TPM arrays for one gene.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gencode_id": {
            "type": "string"
          },
          "tissue_site_detail_ids": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            }
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        },
        "required": [
          "gencode_id"
        ]
      },
      "returns": "Returns, per tissue, the full per-sample TPM array with n_samples — use for distribution analysis rather than medians. gencode_id must be a versioned GENCODE id for the pinned dataset (use gtex_resolve_genes). Omit tissue_site_detail_ids for all tissues (large: ~17k values across 54 tissues in gtex_v8)."
    },
    {
      "name": "gtex_median_expression",
      "description": "Get median gene expression (TPM) for genes x tissues. gencode_ids must be versioned GENCODE ids for the pinned dataset (resolve symbols with gtex_resolve_genes first — wrong-version ids return 0 rows). Omit tissue_site_detail_ids to get all tissues. Fully paged and count-verified; `total` equals the number of (gene, tissue) rows returned.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gencode_ids": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "tissue_site_detail_ids": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            }
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        },
        "required": [
          "gencode_ids"
        ]
      }
    },
    {
      "name": "gtex_multi_tissue_eqtls",
      "description": "Get multi-tissue eQTL meta-analysis (METASOFT) results for a gene.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gencode_id": {
            "type": "string"
          },
          "variant_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        },
        "required": [
          "gencode_id"
        ]
      },
      "returns": "Returns per-variant METASOFT rows with per-tissue m-values (posterior probability of an eQTL effect in each tissue), NES, p-values and standard errors. Without variant_id this can return hundreds of rows (one per tested variant) — pass variant_id to narrow. gencode_id must be versioned for the pinned dataset."
    },
    {
      "name": "gtex_resolve_genes",
      "description": "Resolve gene symbols or unversioned Ensembl ids to versioned GENCODE ids. GTEx expression/eQTL routes require versioned GENCODE ids (e.g. 'ENSG00000111640.14') and the version differs between releases (gtex_v8 = GENCODE v26, gtex_v10 = v39). Pass symbols ('GAPDH') or bare ENSG ids; returns matching reference-gene records with gencodeId, geneSymbol, gencodeVersion and genomeBuild. Call this first, then feed gencodeId into the expression/eQTL tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gene_ids": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        },
        "required": [
          "gene_ids"
        ]
      }
    },
    {
      "name": "gtex_sample_info",
      "description": "Get GTEx sample and donor metadata, optionally filtered. Filters: tissue_site_detail_id (e.g. 'Liver'), data_type (e.g. 'RNASEQ', 'WGS'), subject_id (e.g. 'GTEX-14753'). Returns sample IDs, tissue types, ischemic time, RIN scores, Hardy scale, pathology notes, etc. The full filtered set is walked and count-verified: `total` is the true match count. By default ALL matching samples are returned (an unfiltered call returns tens of thousands of rows — filter, or set max_samples). When max_samples is set, `returned` < `total` and `truncated` is true.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "tissue_site_detail_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "data_type": {
            "type": [
              "string",
              "null"
            ]
          },
          "subject_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "max_samples": {
            "type": [
              "integer",
              "null"
            ]
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        }
      }
    },
    {
      "name": "gtex_single_tissue_eqtls",
      "description": "Get significant single-tissue eQTL associations for a gene and/or variant. Provide gencode_id (versioned, for the pinned dataset — use gtex_resolve_genes) and/or variant_id (GTEx format, e.g. 'chr19_44908684_T_C_b38'); optionally restrict to one tissue. Returns precomputed significant associations with p-values and normalized effect sizes (NES). Fully paged and count-verified.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gencode_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "variant_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "tissue_site_detail_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        }
      }
    },
    {
      "name": "gtex_tissue_sites",
      "description": "List all GTEx tissue sites with metadata for a pinned dataset release.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        }
      },
      "returns": "Returns every tissue site (54 in gtex_v8) with tissueSiteDetailId, display name, sample counts, eGene count and color codes. `total` is the API-verified row count. Use the returned tissueSiteDetailId values (e.g. 'Liver', 'Brain_Cortex', 'Whole_Blood') as the tissue argument of the other gtex_* tools. dataset_id pins the GTEx release (default 'gtex_v8', GENCODE v26/GRCh38; 'gtex_v10' uses GENCODE v39 — gene ids differ between releases)."
    },
    {
      "name": "gtex_top_expressed_genes",
      "description": "Get the top-n genes by median TPM in one tissue (API-side ranking). filter_mt_gene=True (default) excludes mitochondrial genes, which otherwise dominate every tissue. Returns genes in rank order with their median TPM; `total_genes_in_ranking` reports the full ranking size (~56k genes) while `returned` is the requested head.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "tissue_site_detail_id": {
            "type": "string"
          },
          "n": {
            "type": "integer",
            "default": 50
          },
          "filter_mt_gene": {
            "type": "boolean",
            "default": true
          },
          "dataset_id": {
            "type": "string",
            "default": "gtex_v8"
          }
        },
        "required": [
          "tissue_site_detail_id"
        ]
      }
    },
    {
      "name": "panglaodb_cell_types_for_gene",
      "description": "Reverse lookup: which cell types is a gene a marker for? Case-insensitive match on the official gene symbol; set include_synonyms=True to also match pipe-delimited nicknames (e.g. 'K19' -> KRT19). Returns full marker rows plus matched_via ('official symbol' or 'synonym').",
      "inputSchema": {
        "type": "object",
        "properties": {
          "gene_symbol": {
            "type": "string"
          },
          "include_synonyms": {
            "type": "boolean",
            "default": false
          }
        },
        "required": [
          "gene_symbol"
        ]
      }
    },
    {
      "name": "panglaodb_marker_genes",
      "description": "Get cell-type marker genes from the PanglaoDB marker table. Filters (all optional, combined with AND): * cell_type / organ — case-insensitive exact match (enumerate valid values with panglaodb_options). * species — 'Hs' (human) or 'Mm' (mouse); rows tagged for both match either. * sensitivity_min / specificity_max — thresholds on the species-specific sensitivity/specificity columns (human columns when species is omitted); rows with missing values are excluded. * canonical_only — keep only canonical markers. Rows carry gene symbol, nicknames, ubiquitousness index, product description, germ layer, organ and per-species sensitivity/specificity. `total_matching` is the full match count; at most max_rows rows are returned (`truncated` flags a cap — the unfiltered table has 8,286 rows). The dataset is served from a local checksum-verified cache and is frozen upstream since 27 Mar 2020 (a historical snapshot — symbols predate later HGNC/MGI updates).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cell_type": {
            "type": [
              "string",
              "null"
            ]
          },
          "organ": {
            "type": [
              "string",
              "null"
            ]
          },
          "species": {
            "type": [
              "string",
              "null"
            ]
          },
          "sensitivity_min": {
            "type": [
              "number",
              "null"
            ]
          },
          "specificity_max": {
            "type": [
              "number",
              "null"
            ]
          },
          "canonical_only": {
            "type": "boolean",
            "default": false
          },
          "max_rows": {
            "type": "integer",
            "default": 500
          }
        }
      }
    },
    {
      "name": "panglaodb_options",
      "description": "Enumerate valid PanglaoDB filter values: species, organs, cell types.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      },
      "returns": "Returns the distinct species tags, the 29 organs, the 178 cell types, and a cell_types_by_organ index — use these exact strings as panglaodb_marker_genes arguments. (Known upstream data artifacts are reported verbatim: 3 corrupt rows have species '4', 30 have 'None'.)"
    }
  ]
}
