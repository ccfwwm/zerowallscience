/** Compatibility command: normal Python updates publish signed JSON only.
 * An explicitly requested archive release uses publish-python-runtime-archive.mjs.
 */
import './publish-python-dependency-manifest.mjs'
