# Pinned so `scripts/verify-examples.py` stays a real gate rather than a flaky
# one. The committed baselines were produced with exactly these versions, and
# the harness compares floats at 1e-6 relative tolerance; a scipy or numpy
# release that reorders a reduction or changes a curve_fit default can move a
# fitted parameter past that without anything being wrong. Bump these
# deliberately, rerun the harness, and re-commit any baseline that legitimately
# moves.
numpy==2.1.3
pandas==2.3.3
matplotlib==3.10.7
scipy==1.16.3
