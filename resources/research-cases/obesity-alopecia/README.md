# 肥胖—脱发先导案例

这个模板先执行表型、定义、人群、时间和访问条件侦察，再由门禁一冻结脱发表型与验证方案。NHANES 的局部变量检索没有命中明确脱发表型时，只登记为局部缺口，不能推导为官方数据不存在。

每条分支都必须输出实际运行、未运行原因、数据版本、证据范围和下一步缺口。没有匹配 GWAS 时停止 X-Y MR；只有 lead SNP 或缺少必要 LD/区域字段时停止共定位；缺少供者或真实配对信息时降为描述或定位。
