---
name: zerowall-rplatform
description: Use the general rdatalinux R platform for projects, computation, and files.
whenToUse: For R scripts, project operations, workspace uploads, and general statistics.
---

Use `mcp__rplatform__*` for general R computation and project management. Validate project and file paths before execution. Workspace uploads require the ZeroWall upload tool and confirmation. Prefer bounded outputs, pagination, and saved artifact references so large tables do not enter the chat context.
