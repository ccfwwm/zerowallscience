interface:
  display_name: "Automatic Single-Cell Virtual Knockout"
  short_description: "Discover public single-cell data and run auditable scTenifoldKnk workflows"
  default_prompt: "Use $sc-tenifold-knockout to discover suitable public single-cell data, validate raw counts, and plan a reproducible virtual knockout."
