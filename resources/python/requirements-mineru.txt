# ZeroWall's MinerU plugin calls the remote HTTP API from Node. Local document
# extraction is implemented by plugins/files and uses the shared Python
# dependency manifest. No local MinerU Python package/profile is used.
# If local MinerU inference is added later, merge compatible pins into
# requirements-research.txt and regenerate the signed shared dependency list;
# never install this file into a private environment.
