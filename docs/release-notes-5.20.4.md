# ZeroWall Science 5.20.4

本版本聚焦模型上下文去重与诊断，并重新构建桌面运行时和随包插件。

- 在系统提示词最终组装阶段去除相同文本，工具 schema 按公开名称去重，防止多层插件重复贡献相同内容。
- 保持精简常驻工具和 capability_search / capability_execute 按需发现入口，延续 MCP 工具面优化。
- 增加每次请求的安全诊断日志：系统提示词字节数、工具数量和 schema 字节数、消息数量和字节数、surface generation、重复工具调用/结果 ID 数量；指标不记录正文或工具参数。
- 上下文面板明确标注分项为估算值，避免把估算分解与模型用量采样直接比较。
- 同步固定 DSH 提交与插件清单，保留 ZeroWall Python、FigureYa 模块下载和现有科研能力。

## 上下文与升级说明

压缩通过摘要替换当前模型可见历史；历史日志本身不会被删除或改写。当前无状态模型接口仍需每次发送系统提示词、工具和压缩后的可见历史；前缀缓存命中由服务商决定，缓存不等于不占上下文。本版本不宣称已经实现仅发送增量历史，也未修改缓存 token 的归一化公式。

安装后重启软件以加载新运行时。保留用户会话、附件、项目、Python 环境与凭据，不清理历史数据。
