# ZeroWall Science 4.3.8

## 本次更新

- 全面优化项目 README 与中英文系统架构文档，补充产品定位、当前版本边界、典型科研路径和请求数据流。
- 增加科研状态与对话双轨持久化、可恢复异构执行、模型无关权限治理、统一成果谱系、单页级演示修订和证据感知审阅等创新点说明。
- 修正插件数量与演示运行时的架构描述：当前包含 22 个一方领域插件，演示运行时作为 `plugin-presentations` 使用的独立库。
- 明确 4.3.8 演示流程生成 PPTX；历史 PDF artifact 仅保持兼容读取，不再重新生成。
- 完善 Windows 默认执行环境说明，默认使用 PowerShell/pwsh 和 Windows 路径。

## 兼容性与边界

- 保留历史科研数据库、会话和 PDF artifact 的读取兼容性。
- 模型 API、MCP 服务、WSL、SSH 和 Web 资源仍需用户明确配置，并保持独立信任边界。
- 本版本不包含 `.env`、凭据、临时日志或 `.playwright-mcp` 内容。
