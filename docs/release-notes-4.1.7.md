# ZeroWall Science 4.1.7

- 网页搜索现在动态跟随 AI Cloud 当前选中的 DeepSeek 模型与国产模型分组，并通过 DSH `ctx.web` 原生搜索提供方执行请求。
- 启用本机可用的 Codex 与 Claude Code 子代理插件；如果对应命令不存在，运行时仍会安全地报告不可用。
- 更新内置运行时依赖闭包与插件版本，保留 `docs/assets/app-home.png` 资源。
