# ZeroWall Science 4.3.10

## 本次更新

- 修复设置页运行时模型目录调用参数不匹配导致的模型列表报错。
- 保留模型目录中的可用状态、视觉能力状态和已选模型，即使单个提供方检测失败。
- 将文件拖拽提示统一为“附件拖动到此处即可添加”。
- 修复对话停止操作的异步取消处理，停止失败会在会话中显示错误，不再静默卡住。
- 保留普通文件、图片附件和演示文稿工作流，并重新构建桌面运行时。

## 兼容性

- 保留现有 `DSH_HOME`、会话、附件和科研数据库，不迁移用户数据。
- 稳定 Windows 版本通过 `desktop/dist` 生成，使用 ZeroWall Science 稳定更新渠道。
