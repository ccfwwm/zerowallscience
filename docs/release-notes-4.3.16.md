# ZeroWall Science 4.3.16

- 修复旧版或损坏的 MCP 配置在启动迁移阶段抛出异常、导致 Harness 在 Web 服务就绪后退出的问题；单个 MCP 初始化失败现在会被隔离，不再影响桌面端启动。
- 改进桌面端启动故障诊断，异常退出提示会直接给出完整的 `harness.log` 路径，便于定位客户环境问题。
- 新增非肿瘤单细胞虚拟基因敲除工作流，提供 scTenifoldKnk 输入校验、运行规划、执行、状态、结果收集、审核和报告工具，并保留完整运行来源与审核门禁。
- 完善 MinerU 与 R 执行能力，补充配置状态、运行时校验和安全边界；打包审计会验证相关 Host 工具、Skills 与依赖已进入安装包。
- 增加 MCP、MinerU、R 运行时和科研 Skills 的回归测试，提升升级后能力发现与启动稳定性。
