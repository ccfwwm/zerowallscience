# ZeroWall Science 6.2.0 Local Verification

Date: 2026-09-17. Platform: Windows x64. Verification ran against the unpacked packaged application and final installer artifacts.

## Scope And Pins

- The retired local OpenCode plugin was removed. `@jiesou/dsh-opencode-zen-free-provider@0.1.18` is pinned once in every profile and the packaged runtime; the default model remains `deepseek-official/deepseek-v4-flash`.
- DSH remains `0.1.5-rc.2`; the pinned fork commit is `1f75e7706d8709f20dc53929fdf3869d3a67e4ff`, including single-session lifecycle, late catalog probing, complete termination-event and catalog-time fixes.
- Univer: `dsh-univer-office@0.3.2`, telemetry disabled. Existing HuanLin previews retained.
- ManuSift: `2622d024ad27791196eb86bad51a9fe7bb0bb268`; PetCT-derived worker: `7051eb55f611a46db3d9cfa1768e56c7d1a91553`, with ZeroWall integration adaptations.

## Detection And MinerU

`tests/integration/integrity_smoke.py` ran in a clean isolated Python environment with locked core dependencies. Both engines detected exact duplication, reflection, rotation and compression. The scientific core also detected crop reuse. Copy-move evidence located the inserted region; the independent negative fixture had zero image-pair alerts. PDF cross-page detection produced 3 findings; actual cross-paper image matching produced 8 findings. Assertions check source pairs, page locations, evidence files, corrupt inputs, recursive Chinese paths, directory summaries and cached task reuse.

Evidence: `.tmp/integrity-final-620/verification.json`.

The final packaged `python` tool also ran both engines through the real managed embedded Python. Assertions require both engines on exact, flipped, rotated and compressed pairs; 107 merged findings matched the source fixture run. The first packaged attempt exposed an unpacked-worker/ASAR dependency resolution error; this was fixed with an ASAR-anchored `createRequire` and a mandatory packaged worker self-test. A repeat run passed with no detector failures. The result remains partial because intentionally corrupt files and optional OCR checks are disclosed.

Evidence: `.tmp/desktop-research-lab/packaged-python-verification.json`.

`tests/integration/mineru_handoff.py` verified that MinerU Markdown/OCR text replaces native PDF text, its images enter cross-paper detection, and page 1 is retained in evidence. This uses a deterministic MinerU result fixture, not a remote MinerU request.

Evidence: `.tmp/mineru-final-620/verification.json`.

## Actual Desktop

The packaged executable passed ASAR/resource policy, native dependency loading, authenticated startup and live Chinese/English settings checks. Retired plugins are explicitly rejected by archive validation. Full installer limits now account for the bundled Univer Gateway, Viewer, workers and Windows native dependencies.

The packaged Host discovered 6 dynamic OpenCode Zen Free models and completed a real `mimo-v2.5-free` inference. Packaged Desktop screenshots verified the provider in both Settings and the conversation selector, including persisted availability state.

The redesigned packaged splash uses the application version dynamically and displays `v6.2.0`; its 720 px progress surface, 9 px bar and reduced copy were checked at desktop, compact and dark viewports. The packaged Settings dialog reports `6.2.0` and keeps About as the final navigation entry.

Real packaged Univer tools created, modified and exported PPTX, DOCX and XLSX files. ZIP content assertions verified slide text, document text, and worksheet content. The Viewer rendered two canvases and the slide thumbnail/content. Tests used isolated workspace data and the actual Gateway/native runtime.

Evidence: `.tmp/desktop-research-lab/packaged-office-verification.json`, `.tmp/desktop-research-lab/office-preview.png`.

Packaged session deletion passed cancellation, simulated trash failure with recovery, duplicate-click coalescing, busy-target refusal, deletion while an unrelated real Agent was running, noncurrent draft retention, active-session switch and last-session empty state. The Host PID stayed `38108`; main-frame navigation count stayed `0`. Actual system recycling was used except for the intentional failure fixture.

Evidence: `.tmp/desktop-research-lab/packaged-deletion-verification.json`.

Restart verification confirmed the deleted named test sessions remained absent. Evidence: `.tmp/desktop-research-lab/packaged-restart-verification.json`.

## Checks And Limits

- Runtime contracts: 16 passed. Startup splash tests: 2 passed. Base plugin tests: 12 passed with 1 skipped. Desktop and base-plugin typechecks, profile consistency and focused DSH checks passed.
- Full legacy desktop suite: 8 passed, 4 failed. The first failure waits for `Save TSG settings` in the existing settings flow; the following three expect Chinese controls after that interrupted language-switch test. Cleanup also encountered Windows EPERM. The separate packaged startup/settings verifier and targeted Office/deletion checks passed. The complete legacy suite is not claimed green.
- A broader DSH run had 5 failures reproduced on its baseline (122/127 passed); these are recorded in local logs and were not changed as part of this work.
- Large EasyOCR models are optional and absent from the clean test environment. Image-table OCR checks and external reference verification are explicitly marked unexecuted. Remote MinerU service availability was not tested with live credentials.
- The pre-existing managed Python bundle needs the skill's `setup` step for the newly locked detector packages. Setup succeeded in the isolated managed overlay. The machine's configured Aliyun mirror lacked the pinned NumPy distribution; retrying with `PIP_INDEX_URL=https://pypi.org/simple` in the test process succeeded. No global index setting or production Python environment was changed.
- The unified document detector currently targets PDFs; arbitrary non-PDF document detection was not verified. MinerU document artifacts remain available for conversational analysis.
- Session deletion preparation tokens are process-local. A desktop crash while its Host survives can retain a lock until Host restart. Live child-task races and scroll retention did not receive exhaustive desktop coverage.
- Research findings are evidence for review, not an automatic determination of misconduct. Synthetic fixtures do not establish general clinical or publication accuracy.
- No Qiniu upload or GitHub Release. User data, existing Office/PDF/image files, and untracked `.upstream/` were preserved.

## Final Artifact

- Installer: `desktop/dist/zerowall-science-6.2.0-win-x64.exe`
- Size: 333523084 bytes.
- SHA-256: `b33704915ebff5c89629d8bfccfc33286c0b63a54111dfffd2cf4050ee30bca2`.
- Final packaging command: `pnpm package:stable:win`, exit 0 after a complete source build, refreshed runtime preparation and packaged Desktop verification.
- Independent `pnpm verify:package` passed the ASAR policy and both packaged Host/Desktop startup paths.
