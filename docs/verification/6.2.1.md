# ZeroWall Science 6.2.1 Verification

Date: 2026-09-18. Platform: Windows x64. Verification ran against the rebuilt unpacked application and final installer.

## Scope

- Restored the Free Search card under Settings > Plugins > Plugin Configuration by requiring only the stable `slots` service at top level. The optional engine command continues to register through nested `commandUi` injection.
- Removed stale `runUpdate` and `upgrading` runtime references so the Free Search card remains mounted when expanded. The plugin self-updater stays disabled under ZeroWall-managed updates.
- Removed the File Review GitHub Star block, link, localized copy and styles.
- Updated the application and generated first-party plugin manifests to `6.2.1`. The existing default model and OpenCode Zen Free integration remain unchanged.
- Added the Python 3.12.10 background updater, snapshot activation/rollback, accurate inventory refresh, and virtualized per-package dependency management to the local 6.2.1 build.

## Checks

- Clean upstream `dsh-free-search@0.4.28` patch application passed. Patch SHA-256 `498dcee1f79256aefeb19cdcc127a81ff06db502713fa41343e529b3343f21cc` is recorded in all three lockfile references.
- Repository contract tests: 31 passed, 1 existing skip. Free Search runtime contracts: 16 passed. File Review focused client tests: 8 passed. Desktop and File Review typechecks passed.
- `pnpm profiles:check`, `pnpm dsh:verify`, `pnpm dsh:inventory`, and `git diff --check` passed.
- The full source build generated the 6.2.1 installer. The command's final online model gate could not complete because the current OpenCode Zen Free service reported `mimo-v2.5-free` as unavailable; this is an external service check and does not invalidate the local package or Python updater checks below.
- Packaged ASAR/runtime policy, Host/Desktop startup, Settings version/order, and the live Chinese/English Settings switch passed. The OpenCode catalog was present, but its live availability probe was recorded as unavailable rather than treated as a pass.
- The final packaged Python updater E2E passed against `desktop/dist/win-unpacked`: real 983,689,995-byte archive, pause/exit/resume of the same task, navigation during update, 387 effective packages, Python 3.12.10, zero page errors, worker peak 234.39 MiB, search P95 34.10 ms, and main-process event-loop P95 31.59 ms.
- Focused packaged Desktop E2E opened Plugin Configuration, expanded Free Search, verified its configuration body and Bing option, confirmed the File Review GitHub promotion is absent, and continued through Skills and MCP settings.
- The broader upstream File Review suite still contains unrelated Windows permission-mode assertions and omitted upstream publication fixtures; those failures were not treated as this UI change's acceptance path.
- Local validation preceded publication. On 2026-09-18, all six Qiniu desktop release objects and Stable pointers were downloaded and matched against local sizes and SHA-256 hashes before proceeding to GitHub publication.

## Final Artifact

- Installer: `desktop/dist/zerowall-science-6.2.1-win-x64.exe`
- Size: 333759583 bytes (318.30 MiB).
- SHA-256: `8301e502165e01f0604539cdee06fc0e44d8586a292982154045c08dad479546`.
- Final package audit: `.build/repackage-6.2.1-20260918/package-audit-final.json`.
- Final Python updater report: `.build/python-updater/packaged-smoke/verification.json`.

## Publication

- Qiniu installer: https://zerowall.chengxunkeji.cn/stable/releases/6.2.1/zerowall-science-6.2.1-win-x64.exe
- GitHub release target: https://github.com/ccfwwm/zerowallscience/releases/tag/v6.2.1
- Qiniu public verification: `.build/repackage-6.2.1-20260918/qiniu-public-verification.json`.
- Existing Python environment 1.4.0 was also reverified publicly: Python 3.12.10, stable-3 signature, 983,689,995 archive bytes, SHA-256 `ae486bf3a41371bafaf08f8ab16b54b6a398e8c753d4812410ff2af45c51f705`. No replacement Python archive was necessary.
