# ZeroWall Science 6.4.0 验证记录

本次先完成 Windows x64 安装包的本地构建验证，随后按用户指令发布七牛和 GitHub；不替换本机真实用户安装。客户截图来自其他电脑，不能据此确认那台电脑的缓存内容、安装范围或旧进程路径。

## 改动

- 移除 OpenCode 免费模型依赖、补丁、注册、默认配置、运行资源和对应远程测试。历史默认选择迁移到现有默认服务，不注册免费模型适配器；历史对话不删除。
- 更新安装前验证目标版本大于当前版本、文件名、SHA-512 和 EXE ProductVersion。失败时不停止 Host，不退出应用；重复安装点击复用同一请求，普通退出不自动安装。
- NsisUpdater 明确指定当前 EXE 所在目录。安装器恢复原注册范围和目录，支持所有用户安装提权；同名其他目录程序不会被结束。
- 安装器从固定的 electron-builder 26.15.3 模板生成覆盖安装段，不执行旧 Electron 卸载器。只清理随程序交付的专属资源目录，再覆盖新版；保留安装目录的其他文件。仍使用上游卸载器生成机制。
- MinerU 默认开启 OCR，持久化任务批次，支持恢复查询、内容清单 v1/v2，以及 Markdown、图片、表格、页码和 bbox 交接。四个科研技能使用托管 Python、中文路径解码和检查点续跑。

## 已完成验证

- 桌面类型检查通过；桌面单测 21 文件、89 项通过。
- MinerU 11 项测试和类型检查通过；AI Cloud 13 项测试通过，包括两个旧 provider 选择迁移用例。
- Contracts 31 项通过、1 项既有跳过；profile 一致性、插件清单检查通过。
- 冻结锁文件离线验证通过；未升级依赖以绕过镜像问题。
- 托管 Python 3.12.10 / 环境 1.4.0 的 doctor 核心依赖全部通过。
- 真实 MinerU PDF/PNG OCR 验证通过：样本量 n=12、p=0.04、33.3%、表格、页码和 bbox。记录位于 `.tmp/mineru-640-live/scan.pdf.verification.json` 和 `scan.png.verification.json`。
- 科研回归通过：三种中文路径格式、MinerU 表格、图内 OCR、检查点续跑；4044 张图片全局哈希对比 8174946 对，用时 2.797 秒。另一次 4044 张 PNG 图内复制检查用时约 55.078 秒；这些是本机测试耗时，不是性能保证。
- MinerU 交接测试产生 14 条 findings，`partial:true`、`incomplete_execution:false`，明确区分材料覆盖不足和执行未完成。
- 隔离安装器回归通过：独立 appId、中文目录首次安装及覆盖升级均退出 0；省略 `/D` 时恢复原目录，目标旧进程被关闭，其他目录同名进程保持运行，安装目录 sentinel 文件保留，EXE 为 `6.4.0.0`。记录：`.build/installer-test-640/upgrade-verification.json`。初次测试发现的旧卸载器删除 sentinel 问题已修复并复测通过。
- 最终打包 Host 验证通过，包括运行中的插件清单无免费模型插件；最终打包桌面验证通过，启动约 42 秒，关于页为 6.4.0，中英文设置切换通过。日志：`.tmp/host-final-640.log`、`.tmp/desktop-final-640.log`。
- 最终产物内的科研脚本与技能文件共 9 个哈希匹配源码；用最终 Electron worker 和托管 Python 执行 doctor，`missing:[]`、`errors:{}`。探测产生的 Python 字节码缓存曾触发打包策略检查，已移出产物并重新生成干净安装器，再完成桌面验证。
- 使用真实 NsisUpdater 和下载缓存帮助器完成启动交接验证（仅截获最终进程启动，不执行正式安装包）：6.2.0 升级目标明确为 6.4.0 EXE，SHA-512 与二进制版本通过，参数含 `--updated --force-run /D=<原中文目录>`。记录：`desktop/dist/verification-6.4.0/update-handoff.json`。
- 最终更新元数据生成及 `smoke:update` 通过，`git diff --check` 通过。

## 最终本地产物

- 文件：`desktop/dist/zerowall-science-6.4.0-win-x64.exe`
- 大小：334264389 bytes
- SHA-256：`581424dafd975a3f6d4812dffbec05fc98e7003150c5cdb3980676253d2c7a6c`
- 最终安装器构建日志：`.tmp/installer-final-clean-640.log`
- 关于页截图、安装回归、更新交接、科研回归和托管 Python 检查记录：`desktop/dist/verification-6.4.0/`
- 已按用户授权于 2026-09-19 发布七牛 Stable 和 GitHub Latest，公网核验结果见下方记录。当前真实安装未被替换。

## 边界

- 当前测试账户没有管理员权限，所有用户安装的实际 UAC 同意流程未执行；不得将当前用户安装回归表述为 HKLM 实机验证通过。
- 无法直接读取客户机器的旧下载缓存，不能宣称已复现客户“显示新版却启动 6.2.0”的唯一根因。增加的版本/哈希检查和固定原目录行为分别防止错误安装包与错误安装位置。
- 6.4.0 安装后才具有新增的应用内更新校验。旧版客户端不会因发布而自动获得该校验代码，需先升级到本版本。
- 检测 findings 是可追溯的筛查线索，需要人工复核；未启用的可选检测仍在报告中列明。
- DSH 保持 `0.1.5-rc.2`，精确 commit 为 `357e3ff6ca3a4e43e86c2549626c6bde0be5844b`；保留 `.upstream/` 和用户现有数据。

## 复现隔离安装验证

先完成桌面构建和运行资源准备，再从项目根目录执行（不会调用正式产品安装包）：

```powershell
pnpm --filter @zerowallscience/desktop run installer:build
pnpm --filter @zerowallscience/desktop exec electron-builder --config build/electron-builder.installer-test.cjs --prepackaged dist/win-unpacked --win --x64 --publish never
node desktop/scripts/verify-installer-upgrade-640.mjs
```

## 公网发布记录（2026-09-19）

- 七牛：[Windows x64 安装包](https://zerowall.chengxunkeji.cn/stable/releases/6.4.0/zerowall-science-6.4.0-win-x64.exe)。先上传不可变版本文件，再更新三个 Stable 指针并刷新 CDN。六个公网文件均完整下载，大小与 SHA-256 全部匹配本地产物；Stable 版本已核实为 6.4.0。
- GitHub：[v6.4.0 Release](https://github.com/ccfwwm/zerowallscience/releases/tag/v6.4.0)，已设为 Latest，非草稿、非预发布。六个附件均上传完成，GitHub 返回的 SHA-256 和大小全部匹配本地产物。
- 源码 commit：`519bf1f0f74b3c17ad5abcb83c3d4264716f861c`；已推送分支 `codex/6.4.0-research-updater` 和注解标签 `v6.4.0`，远端标签解引用与源码 commit 一致。
- 公网校验记录：`desktop/dist/verification-6.4.0/qiniu-public-verification.json`、`github-server-verification.json`；日志：`.tmp/publish-qiniu-640.log`、`.tmp/verify-qiniu-public-640.log`、`.tmp/verify-qiniu-standard-640.log`。
- GitHub 的五个小附件已完整下载并逐字节匹配本地文件。安装包完整回下载校验未完成：直连、系统代理及分段续传多次超时，保留了已下载分段；服务器 SHA-256 和大小验证通过，但不将完整下载校验标为通过。七牛同一安装包已完整下载校验通过。
