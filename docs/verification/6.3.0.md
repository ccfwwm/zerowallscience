# ZeroWall Science 6.3.0 Verification

Date: 2026-09-18. Platform: Windows x64. The artifact was rebuilt after the final desktop and installer changes.

## Delivered changes

- The installer opens as a modern native Win32/Direct2D screen. It installs for the current user by default; the installation directory and desktop shortcut controls are behind collapsed advanced options.
- Installation phases report preparing, extracting, finalizing and completion through a 14 px progress track with an independent animated light sweep. Reduced-motion mode keeps a slower progress animation.
- Windows uses a frameless integrated title area with red, yellow and green controls for tray hiding, minimize and maximize/restore. The control IPC accepts only the trusted top-level document.
- The installer and startup splash emphasize the complete research workflow, complete literature workflow, intelligent medicine and ready-to-use experience, with dynamic version text and a wide animated track.
- A precise `[data-sidebar-header]` selector replaces the broad window-chrome selector that accidentally constrained the whole sidebar to 92 px. Session history fills the available height and Settings stays at the bottom.
- Archived sessions have a persistent sidebar entry with title/workspace search, content viewing and explicit restoration to the original workspace. Opening an already archived session no longer immediately clears its content.

## Verification

- Desktop typecheck: passed.
- Desktop tests: 20 files, 86 tests passed.
- Window boundary tests: 3 passed. Contract tests: 31 passed, 1 existing skip.
- Exact-process scope test: passed; a same-named executable in another directory remained running.
- Packaged desktop verification: passed. Startup track measured 860 x 14 and changed transform values across samples, including reduced-motion mode. The collapsed sidebar clears the traffic lights (toggle top 40, controls bottom 38). Maximize, restore, minimize and tray hiding passed; tray hiding did not exit the process.
- Isolated installer lifecycle: first install, silent upgrade and silent uninstall all exited 0. The Chinese installation path was used, the sentinel user-data file remained after upgrade/uninstall, and the existing 6.2.1 application process remained running.
- Packaged ASAR/static runtime checks passed before the external model gate. `pnpm smoke:update` passed for 6.3.0 metadata in the initial build; metadata was regenerated for the final installer.
- Final installer welcome and advanced-option screens were opened from the actual EXE and captured under `desktop/dist/verification-6.3.0/`.

The final desktop visual check passed (`desktop/dist/verification-6.3.0/modern-desktop.json`): the startup track measured 860 x 14 px, animated transforms changed between samples, and maximize/restore/minimize/tray controls worked with no renderer errors. The final EXE welcome screen was inspected again after the branding update.

The broader desktop and Host package verifiers cannot complete because `mimo-v2.5-free` is unavailable from the external service (`FreeTierError: free tier can only be used from within OpenCode`). Final logs: `.build/6.3.0-final-package.log` and `.build/6.3.0-final-host.log`. The installer was generated before this verification failure; no provider configuration was changed and full-suite success is not claimed.

DSH targeted navigation tests: 21 passed; API catalog tests: 3 passed. The broader GUI suite recorded 5373 passed, 4 unrelated existing failures and 1 skipped. Existing repository-wide translation pairing issues remain outside this change.

Packaged session-history regression passed (`desktop/dist/verification-6.3.0/session-history.json`): history survived renderer reload and complete Host/application restarts; archived sessions remained searchable and readable after restart; restoration returned the session to its original workspace and persisted. Settings stayed at the bottom in wide and narrow windows, with archive access available on the collapsed rail. Original log bytes were preserved; only Host resume/permission metadata was appended.

## Final artifact

- File: `desktop/dist/zerowall-science-6.3.0-win-x64.exe`
- Size: 334,317,095 bytes
- SHA-256: `d6a064da31dcc8684df342ea8c38bd07e2c68ffc89632db753809befb5f0cbe9`
- Publication: local build only; no Qiniu or GitHub upload was requested.
