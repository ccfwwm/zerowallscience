# ZeroWall Science 6.3.0 Verification

Date: 2026-09-18. Platform: Windows x64. The artifact was rebuilt after the final desktop and installer changes.

## Delivered changes

- The installer opens as a modern native Win32/Direct2D screen. It installs for the current user by default; the installation directory and desktop shortcut controls are behind collapsed advanced options.
- Installation phases report preparing, extracting, finalizing and completion through a 14 px progress track with an independent animated light sweep. Reduced-motion mode keeps a slower progress animation.
- Windows uses a frameless integrated title area with red, yellow and green controls for tray hiding, minimize and maximize/restore. The control IPC accepts only the trusted top-level document.
- The startup splash uses the same research-oriented visual language, dynamic version text, animated reading/knowledge/AI cards and a wide animated track.

## Verification

- Desktop typecheck: passed.
- Desktop tests: 20 files, 86 tests passed.
- Window boundary tests: 3 passed. Contract tests: 31 passed, 1 existing skip.
- Exact-process scope test: passed; a same-named executable in another directory remained running.
- Packaged desktop verification: passed. Startup track measured 860 x 14 and changed transform values across samples, including reduced-motion mode. The collapsed sidebar clears the traffic lights (toggle top 40, controls bottom 38). Maximize, restore, minimize and tray hiding passed; tray hiding did not exit the process.
- Isolated installer lifecycle: first install, silent upgrade and silent uninstall all exited 0. The Chinese installation path was used, the sentinel user-data file remained after upgrade/uninstall, and the existing 6.2.1 application process remained running.
- Packaged ASAR and runtime policy: Host/static checks passed. `pnpm smoke:update` passed for 6.3.0 metadata.
- Final installer welcome and advanced-option screens were opened from the actual EXE and captured under `desktop/dist/verification-6.3.0/`.

The broader package verifier reaches the external OpenCode model gate but cannot complete because `mimo-v2.5-free` is unavailable from the current service (`FreeTierError: free tier can only be used from within OpenCode`). This is recorded in `.build/desktop-package-6.3.0.log`; no provider configuration was changed.

## Final artifact

- File: `desktop/dist/zerowall-science-6.3.0-win-x64.exe`
- Size: 334,283,509 bytes
- SHA-256: `9e9ecd072ef63ec699d50b38a6c291600db5338e7f9012799817c9ed4412e9e4`
- Publication: local build only; no Qiniu or GitHub upload was requested.
