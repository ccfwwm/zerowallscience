# ZeroWall Science 6.6.0

- 注册验证码按钮显示“发送验证码”，提供发送中、发送成功、失败提示及重新发送倒计时。
- 验证码发送和注册校验使用同一服务节点；更换邮箱或节点时清空旧验证码，避免校验对象错配。
- 优化登录注册表单、错误提示、键盘提交和移动端布局，防止重复提交及异步读取覆盖已输入内容。
- 增加“忘记密码”流程，通过 Sub2API 向注册邮箱发送密码重置链接。
- 侧栏账户入口与设置中的 AI 云平台使用同一套登录、注册和密码重置流程。

适用平台：Windows x64。托管 Python 运行时保持 3.12.10。
