# ZeroWall Science 4.2.1

- Recover from isolated Electron safeStorage credential failures without discarding unrelated credentials.
- Show WeChat startup failures with a retry action and clean up failed iLink sessions.
- Keep AI Cloud image models out of the chat model catalog.
- Select `gpt-image-2` only from an AI Cloud group whose Chinese name contains `生图`.
- Order chat model groups as OpenCode Zen, AI Cloud, DeepSeek official, then other providers.
- Add model-catalog synchronization, per-model availability states, login-required diagnostics, and one-click checks for every model.
- Show the synchronized catalog and probe controls in Settings > Models; unavailable routes are visibly disabled in the conversation selector.
- Stack the sidebar footer actions vertically to prevent the lower-right controls from wrapping incorrectly.
- Update the local Windows stable package to 4.2.1.
